# Re:Read — 설계 정본

> 이 문서는 Claude Code가 개발을 진행하기 위한 **단일 기준 문서**다.
> 모호하면 **§2 핵심 기둥**을 우선한다. **새 결정은 코드보다 먼저 이 문서에 반영한 뒤** 진행한다.
>
> 서버 경계는 [`docs/ARCHITECTURE.md`](./docs/ARCHITECTURE.md), 색인·구현 현황은 [`docs/README.md`](./docs/README.md),
> 문서 규율은 [`docs/DOC_DISCIPLINE.md`](./docs/DOC_DISCIPLINE.md), 착수 순서는 [`docs/PLAN.md`](./docs/PLAN.md).
> 사업자·연락처·스토어 정보의 단일 출처는 `C:\project\common\BUSINESS_INFO.md` (**커밋 금지 파일**).
> 포트·에뮬레이터 할당의 단일 출처는 `C:\project\common\DEV_ALLOCATION.md`.
> 커밋 규약은 `C:\project\common\COMMIT_CONVENTION.md`, 문서 체계는 `C:\project\common\DOC_SYSTEM.md`.
> 한국어 문장 규칙은 `C:\project\common\KOREAN_WRITING.md` (**새로 쓰는 글에 적용한다**).

작성일: 2026-09-08 · 원본 기획서("Re:Read — Read it. Remember it. Live it.")를 프로젝트 문서 체계로 옮긴 것.
기획서 원문은 [`docs/ORIGINAL_BRIEF.md`](./docs/ORIGINAL_BRIEF.md)에 보존돼 있고 **정본이 아니다**.

⚠ **레포 디렉터리 이름은 `Bookmind`, 서비스명은 `Re:Read`다.** 초기 작업 디렉터리 이름이 먼저 정해졌고
서비스명이 나중에 확정됐다. 코드·문서·스토어에서 쓰는 이름은 **Re:Read**이며, `Bookmind`는 경로일 뿐이다.

---

## 1. 한 줄 요약

> **Read it. Remember it. Live it.**

책에서 발견한 지식과 생각을 저장하고, AI가 정리하고, **잊을 때쯤 다시 만나게 하고**, 필요한 것만
실제 행동으로 연결해주는 **개인 독서 학습 앱**.

- 장르: 개인 지식관리 / 학습(SRS)
- 플랫폼: Android 우선 → iOS(⚠ 미결정 D)
- 출시 범위: **글로벌 동시 출시**. 기본 언어 **English**, 한국어 동시 제공(§9)
- 독서량을 늘리는 앱이 **아니다**. 한 권에서 얻은 것을 더 오래 남기는 앱이다(기획서 §31).

---

## 2. 핵심 기둥 (설계 충돌 시 판단 기준)

1. **독서를 방해하지 않는다.** 저장은 **원문 하나만 있으면 끝난다.** 책 선택·페이지·내 생각·태그는
   전부 선택이다. 저장 흐름에 필수 입력을 추가하자는 제안은 이 기둥으로 기각한다(기획서 §5·§22 원칙1).

2. **지식의 정본은 사용자의 기기다.** 서버를 정본으로 삼지 않는다 — 서버가 죽어도 저장·복습·실천은
   전부 동작해야 한다. 로그인하지 않아도 앱의 핵심이 완전히 돌아야 한다.

   🔴 **이 기둥은 "평문을 상시 보관하지 않는다"까지만 금지한다. "기기를 떠나지 않는다"가 아니다**
   (2026-09-08 정합 심사로 문안 정정). 실제로 나가는 것: **AI 분석 때 카드가 프록시를 지나간다**(§6) ·
   **v1.1 백업은 암호문을 서버에 보관한다**(`docs/DATABASE.md` §5) · **3차 커뮤니티는 사용자가
   공유를 고른 항목을 서버에 게시한다**(`docs/ARCHITECTURE.md` §7).
   ~~"어느 것도 서버에 저장하지 않는다"~~ 로 쓰면 우리 문서가 스스로 거짓이라 판정한 문장이 된다
   (`docs/ARCHITECTURE.md` §6.2). **판단 기준이 거짓이면 그 기준으로 기각한 제안이 전부 무효다.**

3. **AI는 제안하고 사용자가 정한다.** 실천 생성도, 복습 진행도 강제하지 않는다.
   AI가 "실천 가능"이라 판단해도 사용자가 [넘어가기]를 고르면 그걸로 끝이다(기획서 §22 원칙3).

4. **모든 지식이 실천으로 가지 않는다.** `Knowledge`와 `Practice`를 데이터에서부터 분리한다.
   실천율을 KPI로 올려서 사용자를 밀어붙이는 설계를 하지 않는다(기획서 §22 원칙4·§29).

5. **시험 앱이 아니다.** 복습에 점수·등급·오답률로 압박하지 않는다. 회상 결과는 **다음 노출 시점을
   정하는 입력값일 뿐** 사용자에게 성적표로 돌려주지 않는다(기획서 §22 원칙2).

6. **책보다 사용자의 생각을 중요하게 둔다.** 화면·검색·리포트에서 "내 생각"은 원문과 동등하거나 더
   앞에 온다. 다만 **생각을 쓰지 않아도 카드는 완결**이다(기둥 1과 충돌하지 않게)(기획서 §22 원칙5).

7. 🔴 **"잊을 때쯤 다시 만난다"가 제품의 급소다.** 복습 도달(알림·오늘의 복습)이 실패하면
   나머지가 전부 동작해도 제품은 실패한다. 알림 신뢰성은 기능이 아니라 **가용성 요구사항**으로 다룬다(§8.2).

---

## 3. MVP 범위

### 반드시 구현 (v1.0)

| 영역 | 내용 |
|---|---|
| 책 | 등록(수동) · 목록 · 상세 · 읽기 상태 3종 · 시작/완료일 |
| 수집 | 직접 입력 · 붙여넣기 · **사진 OCR** |
| 지식 카드 | 원문 · 출처(책·페이지) · 내 생각 · 태그 |
| AI | 지식 카드 분석(핵심·개념·주제·유형·실천 가능성) + **회상 질문 5종 생성** |
| 복습 | 오늘의 복습 · 회상(먼저 떠올리기 → 원문 공개) · FSRS 스케줄 · **로컬 알림** |
| 실천 | 실천 생성(사용자 확정) · 반복 주기 · 체크 · 연속일 |
| 통계 | 기본 대시보드(저장·복습·기억률·실천·연속일) |
| 계정 | 구글 로그인 · 탈퇴 · 구독(Premium) · 문의 · 공지 |

### 🔴 MVP에서 제외 (제외가 더 중요하다)

| 제외 | 왜 |
|---|---|
| 커뮤니티 전체(문장 의견·실천 공유·팔로우·좋아요·댓글) | 기획서 §23. 사용자 확보 후 3차(§10) |
| **클라우드 동기화 · 웹 버전** | 2026-09-08 사용자 확정 — 웹 계획 없음. 로컬 정본을 택한 근거(§14 #1) |
| ISBN 검색 · 표지 자동 등록 | 국가별 도서 API가 갈라져 글로벌에서 비싸다. 2차(§24) |
| 음성 입력 | 2차 |
| ~~독서 진행률(%)~~ · 독서 목표 | 🔴 **진행률은 v1.0 에 넣는다**(결정 #17, 2026-09-09) — 총·읽은 페이지 두 값이면 끝이다. **독서 목표(주 N권 등)는 계속 제외** |
| 월간 AI 리포트 · 장기 성장 분석 | v1.1~. 데이터가 쌓이기 전에는 만들 내용이 없다 |
| 광고 | 넣지 않는다(§7) |
| **백업/복원** | 🔴 v1.0 제외이나 **출시 직후 최우선**(§14 #6) — 미루는 것이지 안 하는 것이 아니다 |

### 이후 로드맵

- **v1.1 (출시 직후 최우선)**: 암호화 백업/복원 — 기기 교체 시 전손을 막는다.
- **v1.2~**: 월간 AI 리포트 → 장기 성장 분석 → 개인화 복습(FSRS optimizer).
- **2차**: OCR 개선 · 음성 입력 · 책 검색/ISBN · 독서 목표 · 기존 지식과의 연결.
- **3차**: 커뮤니티(책·문장 단위). 🔴 **로컬 정본과 충돌하지 않는다** — 사용자가 공유하기로 고른
  항목만 올리는 **opt-in 게시** 모델이고, 전체 동기화가 아니다(`docs/ARCHITECTURE.md` §7).

---

## 4. 계정 정책

**기본은 선택적, 특정 기능에서만 필수.**

| 상황 | 로그인 |
|---|---|
| 책 등록 · 지식 저장 · 내 생각 · 복습 · 실천 · 통계 · 공지 읽기 | **불필요** |
| AI 분석 · AI 회상 질문 | **필수**(Premium 게이트와 같은 지점) |
| 구독 결제 · 복원 | **필수** |
| 문의하기 | **필수** |
| 백업/복원(v1.1) | **필수** |

- 로그인은 **구글 소셜 로그인 하나**로 간다. 자체 이메일/비밀번호 회원 시스템을 만들지 않는다.
- 🔴 **로그인을 부팅에 세우지 않는다.** 기획서 §23은 "회원가입/로그인"을 MVP 필수 목록에 넣었지만,
  지식의 정본이 기기에 있으므로 **읽고 저장하는 데는 계정이 필요 없다.** 설치 직후 바로 저장할 수
  있어야 한다(기둥 1). → §14 #3.
- **탈퇴는 선택이 아니다.** 로그인을 붙이는 커밋에 탈퇴를 **같이** 넣는다 — 나중에 붙이면 심사에서 막힌다.
  🔴 **앱 내 경로 + 웹 URL 둘 다 필요하다. 하나만 있으면 반려된다**
  (`common/GLOBAL_DATA_COMPLIANCE.md` §6.1 — 2026-09-08 확인). 웹 URL 은 Play 콘솔에 등록한다.
- 🔴 **계정 삭제 ≠ 구독 해지.** 구독이 있는 앱은 탈퇴 화면에 **별도 해지 안내**를 띄워야 한다
  (같은 문서 §6.1). 계정만 지우고 구독이 살아 있으면 과금이 계속된다 — 그게 분쟁이 된다.
- 처리방침 링크는 **로그인 화면과 설정 양쪽**에 둔다(같은 문서 §7).
- 탈퇴 시 파기 대상은 **서버에 있는 것뿐**이다(subject · 엔타이틀먼트 · AI 사용량 원장 · 문의).
  기기의 지식은 앱 삭제로만 사라진다 — 이 구분을 탈퇴 화면에 명시한다.
  ⚠ `common/ACCOUNT_LIFECYCLE.md` R5: *"되돌릴 수 없습니다"와 "N일 안에 복구됩니다"는 **둘 다 사실이어야** 한다.*
  우리는 **지식이 안 지워진다**는 쪽이라 문구가 더 조심스러워야 한다 — "전부 삭제됩니다"로 쓰면 거짓이다.

### 4.1 무료 한도는 **기기에서 센다** (2026-09-08)

`common/ACCOUNT_LIFECYCLE.md` R1(*"X당 1회 보상은 X의 재생성 가능성을 함께 적는다"*)에 대한 답이다.

- 무료 티어 한도(⚠ 미결정 A)는 **로컬 DB 의 실제 행 수로 센다.** 서버가 세지 않는다.
- 🔴 **그럴 수밖에 없다** — 무료 사용자는 로그인을 안 하므로(§4) 서버에 셀 근거가 없다.
- 🟢 **그래서 탈퇴·재가입 어뷰징이 성립하지 않는다.** 계정과 무관하게 기기 데이터로 세기 때문이다.
  한도를 우회하려면 앱을 지워야 하고, 그 대가는 **그때까지 쌓은 지식 전부**다 — 한도보다 훨씬 크다.
  그래서 `ACCOUNT_LIFECYCLE` 의 유예·복구 설계(R2·R3)는 **우리에게 해당하지 않는다**(재발급될 재화가 없다).
- 연령 게이트: 조각이 겪은 대로 **부팅에 세운다**(기기 식별자 발급 전에 판정). 상세는 v1.0 착수 시
  `docs/ARCHITECTURE.md`에 옮긴다. ⚠ 미결정 G.

---

## 5. 도메인 규칙 (어기면 안 되는 것)

> 스키마 정본은 [`docs/DATABASE.md`](./docs/DATABASE.md).

1. **지식 카드는 원문 하나로 성립한다.** `book_id` · `page` · `thought` · AI 분석 전부 nullable.
2. **AI 분석이 없어도 복습이 돌아야 한다.** 무료 사용자·오프라인·AI 실패 시에는
   **원문 가리기 방식의 기본 회상**으로 대체한다(§14 #9). 복습이 Premium 전용이 되면 기둥 7이 깨진다.
3. **회상 질문은 저장 시 1회 생성해 로컬에 보관한다.** 복습할 때마다 AI를 부르지 않는다 — 비용이
   사용량에 비례해 터지고, 무엇보다 **오프라인에서 복습이 안 된다**(§14 #4).
4. **회상 결과는 채점이 아니다.** 사용자가 스스로 "안 떠올랐다/겨우/떠올랐다/바로"를 고르고, 그 값이
   FSRS 등급이 된다. 정답 문자열 비교를 하지 않는다(기둥 5).
5. **실천은 사용자가 문장을 확정해야 생성된다.** AI가 제안한 문장을 그대로 저장하더라도
   **사용자가 [실천해보기]를 누른 결과**여야 한다(기둥 3).
6. **실천을 지워도 지식은 남는다. 지식을 지우면 연결된 실천은 고아가 되지 않게 함께 처리한다**
   (연결 해제 후 유지 — 상세 `docs/PRACTICE_SYSTEM.md`).
7. **모든 로컬 레코드는 UUID PK + `updated_at` + `deleted_at`(tombstone)을 가진다.**
   자동증가 정수 ID를 쓰지 않는다 — 백업 병합과 장래 동기화에서 되돌릴 수 없는 비용이 된다(§14 #8).
8. 🔴 **FSRS 내부값(`difficulty`·`stability`)을 어떤 화면에도 노출하지 않는다.**
   저장은 하되 보여주지 않는다 — 보여주는 순간 기둥 5가 데이터 층에서 무너진다
   (2026-09-08 정합 심사: *"기둥 5는 문구·색만 금지하고 데이터 층에서 아무것도 금지하지 않는다"*).
   화면에 나가는 것은 **다음 복습 시점**까지다. "이 카드는 난이도 7.2" 같은 표시를 만들지 않는다.

---

## 6. 🔴 서버 경계 (어기면 되돌리기 비싸다)

```
┌─ 기기 (expo-sqlite) ─────────── 정본 ───────────────────────────┐
│  books · knowledge · thoughts · ai_analyses · review_schedules   │
│  review_logs · practices · practice_logs · tags                  │
└─────────────────────────────────────────────────────────────────┘
        │                                   │
        │ 계정·구독·문의·공지                 │ AI 분석 (본문이 지나감, 저장 안 함)
        ▼                                   ▼
┌─ common_server (공용, 이미 운영 중) ─┐   ┌─ Re:Read 서버 (server/, 신설) ─┐
│  auth · devices · entitlements       │   │  /api/v1/ai/analyze            │
│  webhooks/revenuecat · tickets       │   │  ai_calls · ai_usage           │
│  bootstrap(공지·점검·버전) · heartbeat│   │  ai_cooldowns                  │
└──────────────────────────────────────┘   │  (v1.1) 백업 금고 vaults…      │
                                            └────────────────────────────────┘
```

| 무엇 | 어디 | 왜 |
|---|---|---|
| 책·지식·생각·복습·실천 | **기기** | 기둥 2 |
| 신원(subject)·구글 로그인 | **common_server** | 단일 진실. 형제 앱과 공유 |
| 구독 엔타이틀먼트·RevenueCat 웹훅 | **common_server** | 이미 `webhooks/revenuecat/[app]`이 N앱 구조 |
| 공지·점검·버전 게이트·문의 | **common_server** | 앱 무관 공용 기능 |
| AI 프록시 + 사용량 원장 | **Re:Read 서버** | LLM 키를 앱에 넣지 않는다. 캡·쿨다운이 서버에 있어야 우회 불가 |
| 지식 본문 | **어디에도 저장 안 함** | 프록시를 지나가기만 한다 |

AI 처리 경로·무저장 구조·비용 방어의 상세는 [`docs/AI_SYSTEM.md`](./docs/AI_SYSTEM.md).

### 🔴 왜 common_server에 지식을 넣지 않는가

common_server는 **"개인정보 최소수집"을 설계 원칙으로 박아둔 1배포 N앱 공용 서버**다
(테이블: `apps · subjects · entitlements · purchaseEvents · tickets · announcements · subjectActiveDay`).
콘텐츠 저장소가 아예 없고, 넣는 순간 **형제 앱 전부의 처리방침과 폭발 반경이 함께 커진다.**
조각이 같은 판단을 이미 내렸다(`C:\project\diary\docs\ARCHITECTURE.md` §2).

### 🔴 왜 조각(diary) 서버에 편입하지 않는가 (2026-09-08 검토)

조각 서버에는 우리가 원하는 것이 이미 있다(백업 금고 + AI 프록시 + 리퍼 cron). 그래도 **같은 DB에
넣지 않는다.** 실물을 보고 확인한 네 가지:

1. **앱 스코프가 없다.** 조각 서버는 1앱 전제로 지어져 모든 테이블이 `subject_id` 하나로 갈라진다.
   🔴 `uniqueIndex('uq_ai_usage_period').on(subjectId, kind, periodKey)` — 같은 사람이 두 앱을 쓰면
   `(나, 'monthly', '2026-09')`가 충돌해 **한쪽 리포트가 다른 쪽을 막는다.**
2. **폭발 반경이 합쳐진다.** 조각은 출시된 앱이고 실사용자 백업이 들어 있다. 그건 잃으면 못 되찾는다.
3. **탈퇴 파기가 얽힌다.** 파기는 `subject_id` 기준인데, 지금 코드에 "어느 앱의 데이터인가" 구분이 없다.
4. **AI 축이 안 맞는다.** 조각은 `kind(weekly/monthly/yearly) + periodKey` 기간 축이고,
   Re:Read의 주력은 **지식 카드 1건 분석**이라 기간 축이 아니다.

→ **서버는 따로, 코드는 복사 승계한다.** 백업 금고 스키마(`vaults · vault_grants · generations ·
generation_parts · vault_blobs`)는 도메인을 하나도 모르므로 v1.1에 거의 그대로 온다.
겪은 함정(멱등 · `seq+1`만 수용 · `auth_hash`를 `vault_id`와 분리 · 3년 무접근 리퍼 · 구독 만료 유예 90일)이
주석까지 딸려오는 것이 복사의 실제 값이다.

---

## 7. 수익화 정책

**구독 단독. 광고 없음**(§14 #10 — 기획서에 광고가 없고, 기둥 1과 정면으로 부딪힌다).

| | 무료 | Premium |
|---|---|---|
| 책 등록 | 제한(⚠ 미결정 A) | 무제한 |
| 지식 저장 | 제한(⚠ 미결정 A) | 무제한 |
| 복습 | **기본 복습**(원문 가리기) | AI 회상 질문 5종 · 개인화 복습 |
| AI 분석 | ❌ | ✅ (월 캡 — ⚠ 미결정 E) |
| 실천 관리 | ❌ | ✅ |
| 통계 | 기본 | 월간 AI 리포트 · 장기 분석 |

- 결제는 **RevenueCat**, 진실은 **common_server 엔타이틀먼트**(형제 앱 방식 승계).
- 월간/연간 2종. 가격 ⚠ 미결정 B.
- 🔴 **무료에서도 저장과 복습이 성립해야 한다**(§5 규칙 2). 무료를 체험판으로 만들면
  "책을 저장하려고 돈을 내는 게 아니다"(기획서 §28)가 무너진다.
- 법적 의무 세트(구독 표시·해지 경로·환불·미성년자·자동갱신 고지)는 `docs/MONETIZATION_SYSTEM.md`.
  글로벌 출시이므로 EEA/미국 주법 고지도 함께 — `C:\project\common\GLOBAL_DATA_COMPLIANCE.md` 참조.

---

## 8. 도메인 (1) — 지식과 복습

정본: [`docs/KNOWLEDGE_SYSTEM.md`](./docs/KNOWLEDGE_SYSTEM.md) · [`docs/REVIEW_SYSTEM.md`](./docs/REVIEW_SYSTEM.md)

### 8.1 콘텐츠 유형 5종 (AI가 분류)

`FACT` 기억 · `CONCEPT` 이해 · `OPINION` 관점 · `INSIGHT` 회상 · `ACTION` 복습+실천 제안.
유형은 **회상 질문의 종류와 실천 제안 여부를 가르는 스위치**이지 사용자에게 보여주는 라벨이 주목적이 아니다.

### 8.2 🔴 복습 도달 (기둥 7)

- 스케줄러는 **FSRS**(`ts-fsrs`) 기본 파라미터. 기획서 §15의 `1/3/7/14/30/90`은 **사용자 설명용 근사**이지
  구현 스펙이 아니다(§14 #5).
- 알림은 **로컬 알림**(`expo-notifications`)이다. 서버가 무엇을 복습해야 하는지 모르기 때문에
  서버 푸시가 원천적으로 불가능하다 — 로컬 정본의 대가다.
- ⚠ 그래서 **Android 배터리 최적화·Doze로 알림이 조용히 죽는 것**이 이 앱의 최대 단일 위험이다.
  릴리스 빌드에서 실기기 다일차 검증을 v1.0 완료 기준에 넣는다(`docs/PLAN.md` Phase 4).

---

## 9. 도메인 (2) — 다국어

정본: [`docs/I18N_SYSTEM.md`](./docs/I18N_SYSTEM.md)

- **글로벌 동시 출시.** 기본 `en`, 동시 제공 `ko`. 한국은 여러 출시 국가 중 하나다.
- 언어 추가가 **파일 추가만으로** 되도록 키 규약과 `check:i18n` 가드를 **첫날** 세운다.
  조각이 같은 골격으로 15개 언어까지 갔다(`de en es fr id it ja ko pt-BR ru th tr vi zh-Hans zh-Hant`).
- 🔴 **AI 응답 언어는 UI 언어와 별개 축이다.** 책은 사용자의 UI 언어와 다른 언어일 수 있다.
  설정에 전역 하나를 두고 기본값은 "앱 언어와 같음"(조각 승계). 원문은 **책 언어 그대로** 보관한다.
- 날짜·숫자는 기기 로케일. 단 **"하루"의 경계는 기기 로컬 자정 고정**(복습 큐가 흔들리면 안 된다).

---

## 10. 도메인 (3) — 커뮤니티 (3차, 설계만)

정본은 3차 착수 시 `docs/COMMUNITY_SYSTEM.md`. 지금 적어 두는 이유는 **로컬 정본 결정이 커뮤니티를
막지 않는다는 것을 기록해 두기 위해서**다.

- 단위는 사용자가 아니라 **책과 문장**이다(기획서 §26). SNS가 아니다.
- **opt-in 게시 모델**: 사용자가 공유를 고른 항목만 서버로 올라간다. 개인 지식 전체 동기화가 아니다.
- 게시물은 개인 지식 카드와 **별도 테이블**이다. 지우면 개인 카드는 남고 게시물만 사라진다.

---

## 11. 기술 스택

기준 프로젝트는 **`C:\project\diary`(조각)** — 서버 경계·AI 프록시·백업 금고·계정 정책을 승계한다.
앱 골격·문서 방식·커밋 규칙은 **`C:\project\idea_repository`(Idea Repository)** 계보를 따른다.

| 영역 | 선택 | 상태 |
|---|---|---|
| 앱 | Expo SDK 54 · RN 0.81.5 · React 19.1.0 | ❌ 미설치 |
| 언어 | TypeScript ~5.9 (`strict`, `any` 금지) | ❌ |
| 라우팅 | expo-router 6 | ❌ |
| 상태 | Zustand (+ persist) | ❌ |
| **로컬 DB** | **expo-sqlite** `~16.0.10` | ✅ Phase 1(2026-09-09) — 12표 · 러너 · 헬퍼 |
| UUID | expo-crypto `~15.0.9` | ✅ Phase 1 — `randomUUID()`. 🚫 자동증가 정수 금지(결정 #8) |
| 백업 파일 | expo-file-system `~19.0` · expo-sharing `~14.0` · expo-document-picker `~14.0` | ✅ Phase 2(2026-09-09) — 🟢 셋 다 Expo Go 내장이라 재빌드가 없었다 |
| 복습 알고리즘 | `ts-fsrs` | ❌ — §14 #5 |
| OCR | ⚠ 미결정 C — **온디바이스가 기둥 2에서 도출된다** | ❌ |
| 알림 | expo-notifications `~0.32.17` | 🔨 Phase 4(2026-09-09) — 🟢 **Expo Go 에서 로컬 알림 도달 확인**. 🔴 `prebuild` 안 하면 아이콘 리소스가 안 생긴다(My Word 함정) — 그 축은 미확인 |
| **OTA** | expo-updates | 🔨 Phase 4(2026-09-10) · 결정 #18. 🔴 **네이티브 모듈이라 OTA 로 켤 수 없다** → 내부 테스트 첫 빌드에 실어야 한다. `runtimeVersion` 고정 문자열 · 채널 `production` 하나 · 가드 `check:ota`([`docs/OTA_SYSTEM.md`](./docs/OTA_SYSTEM.md)) |
| 서명 | 업로드 키스토어 (`secrets/reread-upload.jks`) | 🔨 2026-09-10 · 🔴 `prebuild` 가 지우므로 **config plugin** 으로 배선한다([`docs/BUILD.md`](./docs/BUILD.md) §5) |
| 구독 | react-native-purchases (RevenueCat) | ❌ |
| 로그인 | @react-native-google-signin/google-signin | ❌ |
| 보안 저장 | expo-secure-store | ❌ — 세션·기기 UUID |
| 다국어 | i18next · react-i18next · expo-localization | ❌ |
| 날짜 | dayjs | ❌ |
| 아이콘 | lucide-react-native | ❌ |
| 폰트 | **시스템 폰트**(번들 폰트 없음 — `assets/fonts/` 를 만들지 않는다) | ❌ — 결정 #13 |
| **Re:Read 서버** | Next.js(App Router) + Drizzle + Supabase Postgres · Vercel | ❌ — 조각 `server/`에서 복사 |
| LLM | Anthropic API (모델 ⚠ 미결정 E) | ❌ |
| 배포 | Expo EAS(앱) · Vercel(서버) · Supabase(DB) | ❌ |

### 자원 할당 (정본은 `common/DEV_ALLOCATION.md`)

| Metro | 서버 | Supabase (api/db/studio) |
|---:|---:|---|
| **8091** | **3500** | **248xx** (24821/24822/24823) |

⚠ 포트를 **반드시 명시**해서 띄운다. 지정하지 않으면 Metro 기본값 8081로 모여 My Word와 충돌한다.

---

## 12. 프로젝트 구조 (예정)

```
Bookmind/
├── CLAUDE.md                  # 설계 정본 (이 문서)
├── docs/                      # 문서 체계
├── app/                       # expo-router 라우트
├── features/                  # 도메인 로직 (books · knowledge · review · practice · ai · support)
├── components/                # 공용 UI (Screen · ListRow · Dialog · …)
├── db/                        # expo-sqlite 스키마 · 마이그레이션
├── lib/                       # 공통 유틸 · common-server SDK 복사본
├── locales/                   # en.json · ko.json
├── theme/                     # 토큰 · 팔레트
└── server/                    # Re:Read 서버 (Next.js · Drizzle · Vercel)
```

의존 방향: `app/` → `features/` → `db/`·`lib/`·`theme/`. **역방향 import 금지.**

---

## 13. 표준 작업 순서

① **플랜** — 상충 결정 확인 + 작업 분해
② **문서** — 설계 결정을 코드보다 **먼저**(`CLAUDE.md` 또는 `docs/<SYS>_SYSTEM.md` + `docs/README.md` 색인)
③ **개발**
④ **검증** — `docs/README.md` §3 검증 루틴 전부 통과
⑤ **커밋** — 문서 갱신을 코드 커밋과 함께(또는 먼저)

- ②를 건너뛰지 않는다. **코드는 틀려도 되지만 문서는 틀리면 안 된다** — 문서가 틀리면 후속 판단이 전부 오염된다.
- ④ 검증 항목은 [`docs/README.md`](./docs/README.md) §3이 정본이다(**스킬에 베끼지 않는다**).
- 검증 안 된 수정은 커밋하지 않고 **되돌린다**.
- ⑤ 🔴 **커밋 형식의 정본은 `C:\project\common\COMMIT_CONVENTION.md`다.** 여기에 베껴 적지 않는다 —
  그 문서의 서문이 지적한 사고가 정확히 그것이다(각 프로젝트가 한 줄씩만 적어 둬서 *"태그를 어떻게 고르고
  본문에 무엇을 쓰는지"* 가 어디에도 없었고, 새 프로젝트마다 형제 로그를 뒤져야 했다).
- 🔴 **화면 시안·UI/UX·문구는 ChatGPT 와 검수한다** — 창구는 **고정 채팅 `Bookmind` 하나**이고
  규칙·제약·함정은 [`docs/DESIGN_REVIEW.md`](./docs/DESIGN_REVIEW.md)에 있다(2026-09-09 사용자 지시).
  🚫 새 채팅을 만들지 않는다 — 그 방에 기획 맥락이 쌓여 있다.
- 브랜치: `main` · `develop` · `feature/*`.
- 🔴 **사용자가 요청하지 않으면 push 하지 않는다.**

---

## 14. 결정 로그 · 미결정

### 확정 결정

**#1 지식의 정본은 기기다 (2026-09-08)** — expo-sqlite에 전부 로컬 저장.
근거: 웹 버전 계획 없음(사용자 확정). 서버 정본으로 가면 앱 기능이 늘 때마다 서버 마이그레이션이
1회씩 붙고 처리방침에 "독서 기록 수집"이 생긴다. 로컬이면 스키마를 바꿔도 서버가 안 바뀐다.
대가: **기기 교체 시 전손** → #6으로 막는다. 멀티 디바이스·웹은 사실상 포기(나중에 하려면 마이그레이션).

**#2 서버는 3분할 (2026-09-08)** — common_server(계정·구독·문의·공지) + Re:Read `server/`(AI 프록시) +
지식 저장소 없음. 조각 서버에 편입하지 않는 이유는 §6에 실물 근거와 함께 기록.
대가: 관리할 서버가 하나 는다. 코드가 복사본이라 관리법은 같다. 추가 요금은 사실상 0(무료 티어).

**#3 로그인을 부팅에 세우지 않는다 (2026-09-08)** — ~~기획서 §23 "회원가입/로그인" MVP 필수~~를 뒤집음.
근거: 지식 정본이 기기에 있으므로 저장·복습에 계정이 필요 없고, 부팅 로그인은 기둥 1과 충돌한다.
로그인은 AI·구독·문의·백업에서만 요구한다(§4).

**#4 회상 질문은 저장 시 1회 생성해 로컬 보관 (2026-09-08)** — 복습할 때마다 AI를 부르지 않는다.
근거: ① 비용이 복습 횟수에 비례해 터진다 ② **오프라인에서 복습이 안 된다** — 기둥 7 위반.
대가: 질문이 고정된다. 질문 다양화·재생성은 2차(기획서 §24).

**#5 스케줄러는 FSRS (2026-09-08)** — `ts-fsrs` 기본 파라미터로 시작.
근거: 구현 비용이 고정 간격표와 비슷한데 결과가 낫고, 개인화(optimizer)로 가는 길이 이미 나 있다.
기획서 §15의 `1/3/7/14/30/90`은 **사용자 설명용 근사**로 강등한다 — 실제 간격은 FSRS가 낸다.

**#6 백업은 v1.0 제외, 출시 직후 최우선 (2026-09-08)** — 🔴 "2차 개발"이 아니라 **v1.1**이다.
근거: #1의 대가가 전손이고, 전손은 앱을 떠날 이유가 된다. MVP 검증 속도를 위해 v1.0에서만 뺀다.
구현은 조각 `server/` 백업 금고를 복사 승계(§6).

**#7 글로벌 동시 출시 · 기본 en + ko (2026-09-08)** — 15개 언어 확장은 프로덕션 출시 전 검토.
근거: 형제 Idea Repository가 `en`+`ko`로 글로벌 출시한 실적이 있다. 키 규약·가드는 첫날 세운다.

**#8 로컬 레코드는 UUID PK + `updated_at` + `deleted_at` (2026-09-08)** — 자동증가 정수 ID 금지.
근거: 백업 병합과 장래 동기화에서 되돌릴 수 없는 비용이 된다. 지금 하면 0원.
반례 근거: My Word가 단일 글로벌 정수 카운터를 써서 정확히 이 지점이 막혀 있다.

**#9 무료 티어에도 복습이 성립한다 (2026-09-08)** — AI 없이 **원문 가리기 방식 기본 회상** 제공.
근거: 복습이 Premium 전용이 되면 기둥 7이 깨지고, "저장하려고 돈 내는 게 아니다"(기획서 §28)도 무너진다.
🔴 **대가(2026-09-08 추가 — 처음에 안 적었다)**: 제품의 경쟁 우위가 **전부 무료 쪽에** 있게 된다.
유료로 남는 것은 AI·실천뿐인데 무료 사용자는 그 둘을 **한 번도 보지 못한다** → #14 로 막는다.
⚠ 다른 결정에는 전부 "대가:"를 적었는데 이것만 빠져 있었고, 그 공백이 정확히 이 문제를 가렸다.

**#10 광고 없음 (2026-09-08)** — 구독 단독.
근거: 기획서에 광고가 없다. 그리고 배너/전면광고는 기둥 1(독서를 방해하지 않는다)과 정면 충돌한다.

**#11 자원 할당 — Metro 8091 · 서버 3500 · Supabase 248xx (2026-09-08)**
`common/DEV_ALLOCATION.md`에 행을 추가했다. 포트를 명시하지 않으면 8081로 모인다.

**#12 이름 (2026-09-08)** — 서비스명 **Re:Read**, 패키지 `com.vivacegames.reread`,
common_server app_code `reread`. 레포 디렉터리 `Bookmind`는 경로일 뿐 제품명이 아니다.

**#13 폰트는 시스템 폰트 (2026-09-08)** — 번들 폰트를 넣지 않는다. `assets/fonts/` 를 만들지 않는다.

🔴 **결정적 근거는 용량이 아니라 "글자가 아예 안 나오는 것"이다.** 이 앱의 본문은 **사용자가 저장한
책 문장**이고, 우리는 **그 언어를 정할 수 없다.** 일본어·중국어·러시아어·아랍어 책을 저장하는 순간
번들 폰트에 그 글자가 없으면 **네모(두부)로 보인다.** 기기의 시스템 폰트만이 그 기기가 가진 글자
전부(Android 는 Roboto + Noto 대체 사슬)를 쓴다. 🔴 **형제 앱에는 없는 제약이다** — 그쪽은 사용자가
임의 언어를 입력하지 않는다. CJK 한 벌만 5~15MB 라 번들로 덮는 것은 애초에 불가능하다.

부차적 근거: ① 용량 — 조각의 Pretendard 3종이 **4.6MB**다. My Word 실측에서 9.78MB 를 줄이자
다운로드가 15초 → 10초가 됐고, 첫 설치 이탈에 직접 걸린다. ② 오픈소스 고지에서 폰트 항목이
**해당 없음**이 된다(Idea Repository `LEGAL_SYSTEM.md` §10 실측). ③ 브랜드는 폰트보다 색·여백·
레이아웃이 더 크게 만든다 — 화면이 0개인 지금 브랜드 방향부터 정해져 있지 않다.

**형제 실측(2026-09-08)**: 6개 중 4개가 시스템 폰트. 번들한 둘은 조각(한국 단일 시장 · Pretendard)과
농구명가(픽셀 게임 · Silkscreen)로 **둘 다 우리 조건이 아니다.** 🔴 **글로벌 출시를 한 유일한 형제
(Idea Repository)가 시스템 폰트다.**

대가: iOS·Android 에서 글자 모양이 다르고 스크린샷이 기기마다 미묘하게 다르다. 브랜드 통일감이 약하다.
→ 되돌리는 비용이 싸다 — 토큰 한 곳(`theme/typography`)만 바꾸면 된다. 반대로 번들로 시작하면
**두부 문제를 글로벌 사용자가 먼저 발견한다.** 재검토 시점은 Phase 11(출시 준비).

**#14 AI 분석 맛보기 — 무료 N건 (2026-09-08)** — 무료 사용자도 **자기 카드로** AI 분석을 N건 받는다.
🔴 **없으면 아무도 안 산다.** 무료 사용자는 AI를 못 보고, 실천은 AI 분석 뒤에 숨어 있고(`actionability`가
분석 출력이다), 백업은 v1.0에 없다 → v1.0에서 돈 받을 이유가 **저장 한도 하나**만 남는데
그건 §28이 금지한 그것이다. 정합·가치 두 렌즈가 독립적으로 같은 결론을 냈다(2026-09-08).
- 개수 N ⚠ 미결정 H. 출발값 3.
- 🟢 **어뷰징이 막힌다** — AI는 로그인 필수(§4)라 **서버가 subject 기준으로 센다.** 재설치로 안 늘어난다.
- 원가는 무시할 수준이다(설치 1인당 N건 1회). 반대로 **안 보여준 것에는 아무도 돈을 내지 않는다.**
- ⚠ ~~"'분석하려면 결제' 자리를 크게 두지 않는다"~~(`docs/KNOWLEDGE_SYSTEM.md` §3)는 **맛보기를
  소진한 뒤에만** 적용한다. 소진 전에는 광고가 아니라 **기능**이다.
대가: 무료 사용자도 원문이 AI 제공자에게 전달된다 → **고지가 무료 사용자에게도 필요하다**(`docs/AI_SYSTEM.md` §2.1).

**#15 로컬 내보내기/가져오기는 무료 · v1.0 (2026-09-08)** — JSON 한 파일 + OS 공유 시트. 서버를 쓰지 않는다.
🔴 **#1의 대가(전손)를 v1.0에서 닫는다.** 백업 금고(#6, v1.1)는 **Premium**이라 그것만으로는
무료 사용자의 전손이 **영원히 안 닫힌다** — 정합 심사가 짚은 공백이다.
근거는 형제 Idea Repository가 같은 자리에서 낸 결론을 승계한다:
> **자기 데이터를 꺼내는 데 돈을 받는 것**이 정면충돌 … 백업은 수익원이 아니라 **이탈 방지 장치**
- 설계·에뮬 실측이 끝난 원본이 있다 — `idea_repository/docs/BACKUP_SYSTEM.md`(내보내기·가져오기·병합/교체).
- 🟢 **기둥 2와 완전히 양립한다** — 파일은 OS 공유 시트로 사용자 손에 가고 앱은 그 뒤를 모른다.
- 🟢 **유료 가치가 안 깎인다** — 유료는 *자동·암호화·클라우드 금고*(#6)이고, 무료는 *수동 파일 한 장*이다.
- v1(평문 JSON)에는 "이 파일에 모든 내용이 그대로 들어 있다"를 고지한다(형제 승계).
대가: Phase 2에 작업이 는다. 그 대신 **v1.0 사용자에게 "지금 지우면 영구 소실"이 더 이상 참이 아니게 된다.**

**#16 AI·실천을 Premium 으로 한 것을 결정으로 등재한다 (2026-09-08)** — 기획서 §23은 "AI 분석"과
"AI 회상 질문"을 **MVP 반드시 구현**으로 지정했다. 우리는 그 둘을 유료로 옮겼는데 **§14에 번호가 없었다.**
제품 전체를 가르는 결정이 결정 로그 밖에 있었던 것이다(정합 심사 지적 — `ORIGINAL_BRIEF.md` 서문의
*"뒤집은 결정은 §14에 근거와 함께 남아 있다"* 약속이 여기서 깨져 있었다).
근거: Premium의 핵심을 **AI + 개인화 학습**으로 잡은 것은 기획서 §28 자신의 결론이다. 유료 표면이
그것 말고 없다. 대가는 #9에 적었고, 무료 사용자의 미체험 문제는 #14로 닫는다.
- 🔴 **단, 사용자가 직접 쓰는 실천은 무료다.** AI가 제안하지 않은 자기 문장 한 줄에 유료 벽을 세우는 것은
  §28 기준선("책을 저장하려고 돈을 내는 게 아니다")과 정면 충돌한다.
  유료인 것은 **AI가 제안하는 실천**(`actionability` 기반)이지 실천 기능 자체가 아니다.

**#17 독서 진행률을 v1.0 에 넣는다 (2026-09-09)** — ~~"2차. MVP는 읽기 상태 3종만"~~ 을 뒤집음.
근거: 사용자 판단 — **총 페이지와 읽은 페이지, 두 값만 받으면 나머지는 자동**이다.
🔴 원래 뺀 이유는 진행률 자체가 아니라 **ISBN·표지 자동 등록이 국가별 도서 API 로 갈라져 글로벌에서 비싸다**는 것이었는데,
진행률은 **그 API 와 아무 상관이 없다.** 빼 둔 근거가 이 기능에는 적용되지 않았던 셈이다.
- 정수 컬럼 둘(`total_pages` · `read_pages`)이면 끝이고, **비율은 파생값이라 저장하지 않는다**(`DATABASE.md` §4).
- 🟢 `DATABASE.md` §2 가 이미 *"넣을 때 컬럼을 덧붙인다"* 고 적어 둬서 설계 변경이 아니라 **예고된 확장**이다.
대가: 마이그레이션 v2 가 생긴다(Expand-only 러너의 첫 실전). 책 등록 화면에 입력 두 칸이 는다 —
⚠ **둘 다 선택이다.** 제목만으로 책이 성립하는 규칙은 그대로다.
🚫 **독서 목표(주 N권·월 N권)는 여전히 제외**다. 그건 사용자를 밀어붙이는 축이라 기둥 5 와 부딪힌다.

**#18 OTA(expo-updates)를 내부 테스트 **첫 빌드**에 넣는다 (2026-09-10)** — Phase 11 항목을 앞당김.
🔴 **근거는 편의가 아니라 소급 불가다.** `expo-updates` 는 네이티브 모듈이라 **OTA 로 OTA 를 켤 수 없다.**
지금 빌드에 안 넣으면 그 빌드가 깔린 기기는 **영원히** 스토어 업데이트로만 고칠 수 있다.
형제 둘이 그 대가를 실제로 치렀다(LinkMemo vc10 잔류자 · 조각은 vc21 에 와서야 탈출구가 생겼다).
🟢 **우리는 사용자가 0 이라 그 대가를 0 으로 치를 수 있고, 이 창은 한 번만 열린다.**
🟢 부수 이득: Phase 4 의 남은 축(알림 실기기 다일차)을 관찰하는 중에 JS 결함을 **재빌드 없이** 고친다.
대가: ① 업로드 키스토어를 지금 만들어야 한다(잃으면 앱을 영구히 업데이트 못 한다. `docs/BUILD.md` §5)
② 🔴 **새 수탁자 Expo, Inc.(미국)가 생긴다** → 처리방침 고지 대상. 지금은 테스터가 우리뿐이라
`common/CLOSED_TESTING.md` §3 으로 성립하고, **Phase 11 완료 기준에 행을 추가해 뒀다**(`docs/OTA_SYSTEM.md` §8).
🚫 **앞당기는 것은 OTA 와 그 빌드뿐이다.** 스토어 등록정보·법무·연령 게이트·R8 은 Phase 11 에 그대로 남는다.

**#19 Phase 6(검색·통계)을 Phase 5(OCR)보다 먼저 한다 (2026-09-10)** — 순서 교환.
🔴 **근거는 취향이 아니라 재설치 비용이다.** OCR 은 온디바이스라(⚠ 미결정 C) **네이티브 모듈**이 붙는다.
네이티브가 바뀌면 새 AAB 를 굽고 폰에 다시 깔아야 하는데, **재설치가 알림 예약을 날리고
Doze 이력을 초기화한다.** 그러면 Phase 4 의 남은 축(실기기 다일차 관찰)이 그때마다 처음부터 다시 시작한다.
🟢 **검색·통계는 순수 JS 와 SQL 이라 OTA 로 나간다.** 재설치가 없으니 관찰이 안 끊긴다.
오늘 OTA 를 붙인 이유가 정확히 그것인데, 바로 다음에 네이티브를 건드리면 스스로 깨는 셈이다.
대가: OCR 이 늦어진다. 🟢 그런데 OCR 은 **저장 경로의 편의**이고 저장 자체는 이미 세 방식이 돌아간다
(직접 입력 · 붙여넣기). 기둥 1 이 요구하는 것은 이미 성립한다.
🔴 **되돌리는 조건**: 알림 다일차 관찰이 끝나면 그 제약이 사라진다. 그때 OCR 로 간다.

### ⚠ 미결정 (7건 — F 는 #13 으로 닫힘)

| # | 무엇 | 왜 지금 못 정하나 | 언제까지 |
|---|---|---|---|
| A | 무료 티어 한도 수치(책 N권 · 지식 N개) | 실제 사용 밀도를 모른다. 너무 낮으면 체험판이 되고(#9 위반) 너무 높으면 전환이 없다 | **Phase 10** |
| B | Premium 가격(월/연) | 글로벌 가격이라 국가별 티어 검토 필요. `common/BUSINESS_INFO.md` 확인 후 | **Phase 10** |
| C | OCR 엔진 | 온디바이스 방향은 기둥 2에서 확정. 구체 라이브러리는 Expo SDK 54 호환성 실측 필요 | **Phase 5** |
| D | iOS 동시 출시 여부 | 형제 앱은 전부 Android 선행. 비용·심사 부담 확인 필요 | **Phase 11** |
| E | AI 모델 · 월 분석 캡 | 카드 1건 분석 원가를 재기 전에는 캡을 못 정한다. 기본값 `claude-opus-5` | **Phase 8** 실측 후 |
| ~~F~~ | ~~폰트~~ | ✅ **닫힘 — 결정 #13(2026-09-08)**: 시스템 폰트 | — |
| G | 연령 게이트 기준 | 지역별 16/14/13. 조각 방식 승계 예정이나 우리 데이터 성격에 맞는지 확인 필요 | 🔴 **Phase 7** — 로그인보다 앞에 서야 하므로 Phase 11이면 부팅 순서 재작업이다(`common/GLOBAL_DATA_COMPLIANCE.md` §5) |
| H | AI 맛보기 개수 N (결정 #14) | 원가 실측 전이라 못 정한다. 출발값 **3건**. 너무 적으면 가치를 못 느끼고 너무 많으면 살 이유가 준다 | **Phase 8** |

---

## 15. 선행 의존 (앱 코드 밖 작업)

> 🔴 **여기에 없는 항목은 "모르는 것"이지 "안 된 것"이 아니다**(`common/DOC_SYSTEM.md` §1).
> 인허가·계정·등록의 정본은 `common/BUSINESS_INFO.md`다. 거기 없으면 행을 만들고 `❓`로 두고 **묻는다.**

| 항목 | 상태 | 비고 |
|---|---|---|
| common_server `apps`에 `reread` 등록 | ❌ | `node tools/seed.ts reread "Re:Read"` (common_server에서 실행) |
| common_server SDK 복사 → `lib/common-server/` | ❌ | `common_server/client/{index.ts,types.ts}` **복사**. 수정 금지, 갱신은 재복사 |
| Discord 문의 웹훅 env + 재배포 | ❌ | `DISCORD_TICKET_WEBHOOK_URL_REREAD` |
| Supabase 프로젝트(Re:Read 전용) | ❌ | 🔴 `supabase init`이 `project_id`를 **디렉터리 이름**으로 잡는다 — `server/`면 형제와 겹친다(`DEV_ALLOCATION.md` §0) |
| Vercel 프로젝트(Re:Read 서버) | ❌ | |
| Anthropic API 키 | ❌ | 서버 env에만. 🔴 앱에 넣지 않는다 |
| RevenueCat 프로젝트 + 상품 등록 | ❌ | 월간/연간 2종. 가격 ⚠ 미결정 B |
| Google Sign-In OAuth 클라이언트 | ❌ | Play 서명 키 SHA-1 필요 |
| ~~Play 콘솔 앱 생성~~ | ✅ **2026-09-10** | **Re:Read** · 앱 ID `4973508670760124973` · 기본 언어 **en-US**(결정 #7) · 앱 · 무료. 🔴 패키지명은 그 항목에 **영구 고정**됐다 |
| ~~서비스 계정에 Re:Read 앱 추가~~ | ✅ **2026-09-10** | 🔴 **새 앱은 안 딸려 왔다**(문서가 예고한 그대로 403). 앱을 붙이고 `앱을 테스트 트랙으로 출시` 를 켜서 업로드한 뒤 **회수했다**(권한 5 → 4) |
| ~~내부 테스트 트랙 · 테스터 목록~~ | ✅ **2026-09-10 16:14 게시** | `활성` · 내부 테스터에게 제공됨 · 검토되지 않음. 테스터는 기존 목록 **`사장님 검증 전용`**(2명) · 🔴 doply(41명)는 안 붙였다. 참여 링크 `play.google.com/apps/internaltest/<내부 테스트 ID>` |
| ~~EAS 프로젝트~~ | ✅ **2026-09-10** | `@shs00925/reread` · `8785afeb-e523-40c5-9b42-20593214aba6`. OTA URL 의 근거값(`docs/OTA_SYSTEM.md` §4) |
| ~~업로드 키스토어~~ | ✅ **2026-09-10** | `secrets/reread-upload.jks` · 🔴 잃으면 앱을 영구히 업데이트 못 한다(`docs/BUILD.md` §5) |
| 처리방침·약관 게시 | ❌ | 글로벌이라 EN·KO 병기. `common/GLOBAL_DATA_COMPLIANCE.md` |
| 🔴 **계정 삭제 안내 웹 URL** | ❌ | §4 — 앱 내 경로만으로는 **반려된다.** Play 콘솔에 등록 + 200 실측. 조각 `docs/delete-account.html` 참고 |
| 상표 확인 — "Re:Read" | ❓ | 사용자 확인 필요. 흔한 단어 조합이라 선사용 여부 확인 |
| ~~**책담**(`com.chaekdam.app`) 처분~~ | 🚫 **무관** | ✅ **닫힘(2026-09-08 사용자 확인 — `common_server` 세션 경유)**: 책담은 **중단한 프로젝트**이고 Re:Read와 무관하다. 패키지도 다르다(`com.vivacegames.reread`). → 이름 결정 #12 유지 · 책담 처분은 **우리 일이 아니다**(Play 임시라 게시된 적 없음, 계정 정리 시 `common/PLAY_CONSOLE_STATUS.md` §2-2에서 함께 본다) |

---

## 16. 현재 상태 (2026-09-09 · 하루 종일 작업)

**문서 체계 + 기획 검증 + Phase 0 + Phase 1 + Phase 2(CRUD) 완료.**
에뮬레이터에서 화면 6개가 돌고, 저장·수정·삭제가 로컬 DB에 실제로 남는다.

- ✅ `CLAUDE.md` §1~§16 · `docs/` 12종 · 기획서 원문 보존 · 스킬 4종
- ✅ 서버 경계 확정(§6) — 실물 조사(common_server 스키마 · 조각 서버 스키마) 근거 포함
- ✅ 확정 결정 **16건** · ⚠ 미결정 **7건** (세는 법은 [`docs/README.md`](./docs/README.md) §1)
- ✅ **기획 검증(2026-09-08)** — `devils-advocate` 4렌즈 독립 심사.
  🔴 세 렌즈가 겹쳐 짚은 Phase 번호 드리프트 37곳 정정 · 결정 #14~#16 신설
- ✅ **Phase 0(2026-09-08)** — Expo 부트 · 토큰 · i18n · 가드 3종(전부 SELF-TEST 내장).
  변이 6종 FAIL 발화 확인 · 번들 200(1.66MB) · 🔴 번들에 폰트 참조 0건(결정 #13 코드 검증)
- ✅ **Phase 1(2026-09-09)** — 로컬 DB v1. 12표 · 인덱스 12 · 마이그레이션 러너 · UUID/tombstone 헬퍼 ·
  §3 삭제 규칙. 🔴 **`db/` 를 순수 모듈로 갈라 `check:db` 가 실물 SQLite 에 같은 스키마·같은 러너를
  세워 잰다**(`docs/DATABASE.md` §6) — 변이 9종 전부 발화.
  🔴 착수 전 대조에서 **문서 모순 셋**이 나와 코드보다 먼저 고쳤다(§1.1 예외 넷 · §1.4 되살리기 신설)
- ✅ **Phase 2(2026-09-09) 완료** — 홈·빠른 저장·문장 목록/상세·책 목록/등록/상세·**백업(내보내기/가져오기)**.
  🟢 **AVD `reread`(5574) · Expo Go E2E** — 저장 → 목록 → 상세 → 생각 추가 → 책 연결 →
  책 삭제(문장 생존) → 문장 삭제, 그리고 **내보내기 → 공유 시트 → 가져오기(합치기·두 번 넣어도 동일)** 까지 밟았다.
  🔴 결정 #15 로 **무료 사용자의 전손이 v1.0 에서 닫혔다**(`docs/BACKUP_SYSTEM.md`)
- 🔨 **화면 문구 전수 검수(2026-09-09)** — ChatGPT 창구(`docs/DESIGN_REVIEW.md`)로 88키 검수.
  복습 4등급이 `쉽다`(문제의 난이도)에서 `바로 떠올랐다`(회상 경험)로 바뀐 것이 가장 크다
- 🔨 **Phase 4 알림(2026-09-09)** — 예약 계산(순수·가드 12종 경계) · 설정 스위치 · 시각 5종.
  🟢 **에뮬레이터에서 실제로 도달했다.** 🔧 켜자마자 예약이 **두 배**가 되던 결함을 실측이 잡았다
  (`REVIEW_SYSTEM.md` §6.5 — 화면으로는 안 보인다).
  ✅ **아이콘 넉 장**을 만들어(`npm run icons`) §6.3 의 세 축을 닫았다 — manifest·drawable 5종·색상값.
  ⏸ Doze·강제 종료·재부팅은 **실기기 다일차** 축이라 그대로 열려 있다
- ✅ **실기기(갤럭시 S24)에 릴리스 빌드 설치(2026-09-09)** — `docs/BUILD.md` 신설.
  🔴 **개발 빌드는 며칠짜리 관찰에 틀린 도구다**(PC 의 Metro 에 묶인다) → 릴리스로 옮겨 5분에 끝났다

### 오늘(2026-09-09) 찾은 결함 다섯 — 전부 **화면은 멀쩡한데 틀린 것**

| 무엇 | 잡은 것 |
|---|---|
| 죽은 i18n 키 | `check:i18n` 축 ⑥ 신설(두 언어에 대칭이면 대조 검사가 침묵한다) |
| 소스의 NUL(0x00) | `check:chars` — **바이트를 읽는 유일한 가드** |
| 알림 이중 예약(7일 → 14건) | `adb shell dumpsys alarm` |
| 순환 import | **Metro 번들 로그** |
| 키보드가 입력창을 가림 | **실기기**(에뮬은 마우스라 키보드가 안 뜬다) |

🔴 **다섯 다 `tsc`·`lint`·가드가 초록이었다.** 동작을 재는 검사는 전부 놓쳤고,
**다른 층을 읽는 것**(바이트 · 기기 상태 · 번들 로그 · 실기기)이 잡았다.
★ 같은 문장이 하루에 다섯 번 나왔다 — **"확인했다" 와 "확인할 수 있는 상태였다" 는 다르다.**

### 2026-09-10 — 내부 테스트 + OTA 배선 (결정 #18)

**목적은 배포가 아니라 Phase 4 다.** 알림의 남은 축은 실기기 다일차 관찰인데, 손으로 설치한 APK 로 하면
JS 결함을 고칠 때마다 **재설치가 관찰을 리셋한다**(예약이 날아가고 Doze 이력이 초기화된다).
내부 테스트 트랙 + OTA 로 옮기면 그 리셋이 없어진다.

- ✅ EAS 프로젝트 `@shs00925/reread` (`8785afeb-…`) · **업로드 키스토어**(`secrets/reread-upload.jks`)
- ✅ `expo-updates` · `app.json` `updates` 블록 · **서명 config plugin** · `eas.json` · `npm run build:aab`
- ✅ **가드 `check:ota` 7축.** 변이 **10종 전부 발화**(판정 함수를 죽이면 **exit 2**)
- ✅ 문서 [`docs/OTA_SYSTEM.md`](./docs/OTA_SYSTEM.md) 신설 · [`docs/BUILD.md`](./docs/BUILD.md) §5~§7
- ✅ **첫 AAB: `BUILD SUCCESSFUL in 20m 1s` · 53.4MB · vc1 `0.1.0`**.
  🔴 서명 주체가 **업로드 키**임을 확인(`Owner: CN=Re:Read …` · SHA1 일치) · 채널·URL·ABI 4벌 확인.
  보관 `D:\builds\Bookmind\reread-vc1.aab`(해시 대조)
- ✅ **Play 콘솔 앱 생성 · 권한 · 업로드까지 끝냈다**(사용자 승인 뒤 크롬으로).
  `internal` 트랙 · **status `draft`** · vc1 · sha1 `6725fa38…` · 🔴 **프로덕션 릴리스 0건 확인**.
  진단이 `404 → 403 → 200` 으로 **한 단계씩** 움직이는 것을 봤다(`docs/BUILD.md` §7.0).
- ✅ **권한 회수 완료**(권한 5 → 4). 출시 권한을 상시로 두지 않는다
- ✅ **게시까지 끝냈다**(16:14). 🔴 **폰에는 `com.vivacegames.reread (unreviewed)` 로 뜬다.**
  앱 검토 전까지 임시 이름이고 고장이 아니다. 이름은 Phase 11 의 등록정보와 함께 붙는다

🔴 **거짓 초록을 우리도 재현했다.** 이 서비스 계정은 RevenueCat 용이라 읽기 권한이 상시다.
**권한을 회수한 뒤에도 `check:play-access` 는 200 을 유지했다.** 조각이 속았던 그 자리이고,
이제 우리 저장소에 **실측으로** 적혀 있다. 판정은 `eas submit` 이 통과하는 것뿐이다.

🔴 **지금 하는 이유는 미루는 비용이 다른 항목과 성질이 다르기 때문이다.**
`expo-updates` 는 네이티브 모듈이라 **OTA 로 OTA 를 켤 수 없다.** 지금 빌드에 안 넣으면
그 빌드가 깔린 기기는 **영원히** 스토어 업데이트로만 고칠 수 있다. 형제 둘이 그 대가를 치렀고
(LinkMemo vc10 잔류자 · 조각은 vc21 에 와서야 탈출구가 생겼다) **우리는 사용자가 0 이라 0 으로 치른다.**
이 창은 한 번만 열린다.

🚫 **함께 앞당긴 것은 OTA 와 그 빌드뿐이다.** 스토어 등록정보·법무 문서·연령 게이트·R8 은
Phase 11 에 그대로 있다. 내부 테스트는 **테스터가 우리뿐**이라 그것들 없이 성립한다
(`common/CLOSED_TESTING.md` §3).

### 2026-09-10 (이어서) — Phase 6 검색 · 통계 (결정 #19)

OTA 가 붙어 있으니 **순수 JS 로 끝나는 것부터** 한다. 검색과 통계는 무선으로 나간다.

- ✅ 문서 먼저 — [`docs/STATS_SYSTEM.md`](./docs/STATS_SYSTEM.md) 신설 · `KNOWLEDGE_SYSTEM.md` §6 확장(§6.6 화면)
- ✅ 순수 모듈 다섯 — `features/search/{sql,repo}.ts` · `features/stats/{compute,sql,repo}.ts`.
  🔴 `sql.ts`·`compute.ts` 는 expo 를 안 쓰므로 **가드가 같은 코드를 node 에서 돌린다**
- ✅ 화면 둘 — `app/search.tsx`(디바운스 350ms · 자동 포커스 · 맞은 축 표시) · `app/stats.tsx`
- ✅ 진입점 — 홈 헤더 `검색`(문장이 있을 때만) · 홈 아래 줄 `연속 N일`(0이면 `통계`)
- ✅ **가드 둘 · 변이 20종 전부 발화**(`check:search` 10 · `check:stats` 10). `verify` 는 이제 **열하나**다
- ✅ **에뮬 `reread`(5574) 실측** — 화면 값과 기기 DB 재계산이 전부 일치했다(`docs/PLAN.md` Phase 6 표).
  🔴 `%` 하나로 검색해 **0건**을 봤다. 와일드카드가 새면 전부가 나왔을 자리다

### 🔴🔧 변이 주입이 찾은 것 — 틀리면 **멈춘다**

`previousDayKey` 에서 `d - 1` 을 `d` 로 바꾸는 변이 하나에 가드가 **실패하지 않고 30분을 돌았다.**
`streakDays` 의 반복에서 커서가 안 줄어 무한 반복이 된다.

🔴 **가드가 아니라 앱의 문제다.** 실기기에서 같은 일이 나면 통계 화면이 느려지는 게 아니라
**JS 스레드가 멈추고 앱 전체가 언다.** 숫자가 틀린 것과 앱이 서는 것은 등급이 다르다.
`streakDays` 에 종료 보장을 박고, 변이 스크립트에 `timeout` 을 걸어 **멈춤을 침묵이 아니라 사고로** 센다
([`docs/EDGE_CASES.md`](./docs/EDGE_CASES.md) §12).

★ **§16 의 "화면은 멀쩡한데 틀린 것" 다섯 건보다 한 칸 더 나쁜 자리다.** 그리고 이번에도
`tsc`·`lint`·가드는 전부 초록이었다. 찾은 것은 테스트가 아니라 **변이 주입**이다.

### 🔴 지금 열려 있는 것

0. 🔴 **키보드 수정을 실기기에서 못 봤다.** 코드·문서·빌드는 끝났는데 폰이 무선 디버깅에서 끊겨
   설치를 못 했다. **판정 전까지 "고쳤다"고 적지 않는다** — 오늘 같은 자리에서 두 번 틀렸다
   (`KeyboardAvoidingView` 가 코드에 보여서 · Metro 가 200 을 줘서).
1. **상표 확인 — "Re:Read"** (§15 `❓`). 흔한 조합이다. **지금이 이름을 바꾸는 비용이 가장 싼 시점**이고,
   스토어 등록 뒤에는 패키지·app_code·법무 페이지·아이콘까지 걸린다.
2. ✅ ~~에뮬레이터 렌더 · expo-sqlite 실개봉~~ — **둘 다 2026-09-09 에 닫혔다.**
   ⚠ 대신 **실기기는 아직이다.** 에뮬은 Doze·배터리 최적화를 재현하지 않으므로
   Phase 4(알림)의 완료 기준은 여전히 실기기 다일차 검증이다(§8.2).
3. ⚠ **미결정 7건** — 전부 해당 Phase 에 마감이 걸려 있다. 지금 막는 것은 없다.

### 다음 단계

🔴 **먼저 할 것: 폰에 릴리스 APK 를 설치하고 세 가지를 한 번에 본다.**
① 아이콘 ② 키보드에 입력창이 안 가리는가(문장 저장 화면의 아래쪽 입력) ③ 문장 5~10개 저장 + 알림 켜기.
③ 을 하면 **Phase 4 의 남은 축(며칠짜리 관찰)과 "AI 없이 쓸 만한가" 판정이 동시에 시작된다.**
⚠ 무선 디버깅이 하루에 세 번 끊겼다. **USB 가 확실하다**(`docs/BUILD.md` §3).

그 다음은 [`docs/PLAN.md`](./docs/PLAN.md) **Phase 5 — OCR**(Phase 6 은 2026-09-10 에 닫혔다),
또는 보류해 둔 화면 전체 패스
(좌우 여백 16 → 32 · 헤더 로고 22 → 32 — 둘 다 `Screen`·`Header` 공용이라 한 번에 한다).
✅ Phase 3 의 ⏸ 였던 **비행기 모드 복습**은 2026-09-09 에 닫혔다 — 오프라인에서 전 과정이 돌고
DB 에도 남는다(`EDGE_CASES.md` §7.2). ⏸ 남은 것은 **사용자 판정**("AI 없이도 쓸 만한가")뿐이다.
