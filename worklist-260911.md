# 2026-09-11 작업 목록 — 2026-09-10 세션 인계

⚠ **이 파일은 2026-09-10 세션이 만들어야 했는데 안 만들었다.** `common/WORKLIST_HANDOVER.md` 가
바로 그날 신설됐는데, 그 세션은 규칙을 읽지 않고 `CLAUDE.md` §16 만 고치고 닫았다.
2026-09-11 문서 재적재에서 발견해 그날 아침 소급 작성했다. §2 의 날짜가 어제인 이유가 이것이다.

---

## 0. 바로 시작할 것 — 폰의 vc1 을 지우고 Play 에서 vc2 를 받는다 (🙋 사용자 승인 대기)

- **설계 정본**: `CLAUDE.md` §16 "지금 열려 있는 것" 0번 · [`docs/BUILD.md`](./docs/BUILD.md) §3(무선 디버깅 · 서명 불일치)
- **재현·검증**(먼저 이것부터. 그 사이 상태가 바뀌었을 수 있다):

  ```bash
  adb -s <기기> shell dumpsys package com.vivacegames.reread \
    | grep -E "versionName|versionCode|installerPackageName"
  ```

  지금은 `0.1.0 / 1 / installerPackageName=null` 이 나온다.
  받은 뒤에는 `0.2.0 / 2 / installerPackageName=com.android.vending` 이어야 한다.

- 핵심:
  1. 폰에 있는 것은 2026-09-09 21:20 에 **손으로 깐 릴리스 APK** 다(디버그 키로 서명됐다).
  2. Play 의 vc2 는 Play 앱 서명 키라 **서명이 달라 덮이지 않는다.** 게다가 그 빌드에는
     `expo-updates` 가 없어서 **OTA 도 안 닿는다**(OTA 배선은 그 다음 날 커밋 `15776ed` 로 들어왔다).
     즉 그 기기를 고칠 통로가 **0** 이다.
  3. 저장된 문장이 **0개**라 지워도 잃을 것이 없다. 2026-09-10 밤에 눈으로 확인했다.
  4. 받는 곳은 내부 테스트 참여 링크다: `play.google.com/apps/internaltest/<내부 테스트 ID>`
  5. 깔면 **한 번에 넷을 본다.** 🔴 OCR · 🔴 키보드 · 책 select 와 `+ 새 책 등록` · 검색·통계·실천.
     그리고 **문장 5~10개 저장 + 알림 켜기**로 며칠짜리 관찰이 시작된다.

- 주의:
  - 🚫 **앱 삭제를 우리가 하지 않는다.** 사용자 승인이 필요하고, 승인받아도 지우는 것은 되돌릴 수 없다.
  - 🚫 잠금 화면을 풀지 않는다. 🚫 폰 시계를 돌리지 않는다.
  - ⚠ 무선 디버깅 포트는 껐다 켤 때마다 바뀐다. 매번 `adb mdns services` 로 다시 읽는다.
  - ⚠ 폰이 잠들면 연결이 끊긴다. 붙어 있는 동안 화면을 켜 둔다.
  - ⚠ 폰에 앱 이름이 `com.vivacegames.reread (unreviewed)` 로 뜬다. 앱 검토 전까지의 임시 이름이고 고장이 아니다.

---

## 1. 백로그

### 유저 피해가 실재

없음. 사용자가 0명이고 내부 테스터 2명(우리)이 전부다.

### 보안·수익

| 순위 | 항목 | 문서 | 왜 급한가 |
|---|---|---|---|
| 1 | 🙋 **상표 확인 "Re:Read"** | `CLAUDE.md` §15 `❓` | **지금이 이름을 바꾸는 비용이 가장 싼 시점**이다. 패키지명이 Play 항목에 영구 고정돼 있어, 스토어 등록 뒤에는 app_code·법무 페이지·아이콘까지 함께 걸린다 |
| 2 | Phase 10 구독(RevenueCat · 가격) | [`docs/MONETIZATION_SYSTEM.md`](./docs/MONETIZATION_SYSTEM.md) · ⚠ 미결정 A·B | 🔴 콘솔 상품 등록이 결제 빌드를 전제하고 약관 게시가 그보다 앞에 있다. 순서를 모르고 들어가면 Phase 10 에서 막힌다 |

### 제품·검증 도구

| 순위 | 항목 | 문서 | 왜 급한가 |
|---|---|---|---|
| 1 | 🔴 **OCR 인식 미판정** | [`docs/KNOWLEDGE_SYSTEM.md`](./docs/KNOWLEDGE_SYSTEM.md) §2.1 | 어제 만든 것 중 **유일하게 안 밟은 축**이다. 네이티브 모듈이라 에뮬 Expo Go 로는 원리상 못 본다 |
| 2 | 🔴 **키보드 가림 미판정** | [`docs/EDGE_CASES.md`](./docs/EDGE_CASES.md) §11 | 고친 코드가 폰에 닿은 적이 없다. 폰의 빌드가 그 수정을 담았는지도 버전으로 갈리지 않는다. 🔴 판정 전까지 "고쳤다"고 적지 않는다 |
| 3 | 🔴 **Phase 4 알림 다일차 관찰** | [`docs/PLAN.md`](./docs/PLAN.md) Phase 4 · [`docs/REVIEW_SYSTEM.md`](./docs/REVIEW_SYSTEM.md) §6.1 | 하루짜리 테스트로는 안 잡히는 종류다(Doze · 강제 종료 · 재부팅). 🟢 이제 네이티브가 다 들어가 있어 관찰 중 JS 결함은 OTA 로 고친다 |
| 4 | 🙋 **Phase 3 사용자 판정**("AI 없이도 쓸 만한가") | [`docs/PLAN.md`](./docs/PLAN.md) Phase 3 ★ | 🔴 미달이면 다음 Phase 로 가지 않고 복습 설계를 다시 한다. 기억의 감쇠는 시뮬레이션이 안 되므로 실제로 저장한 문장이 며칠 흐려져야 한다 |
| 5 | ⏸ **OCR 줄 선택 화면**(결정 #22) | [`docs/KNOWLEDGE_SYSTEM.md`](./docs/KNOWLEDGE_SYSTEM.md) §2.2 · §2.2.1 | 순수 함수와 가드 축 ⑥⑦⑧ 은 2026-09-11 에 세웠다. 🔴 **화면은 위 1번(vc2 인식 판정) 뒤에** 얹는다. 변수를 둘 한꺼번에 넣으면 실패했을 때 원인을 못 가른다 |

### 표면 흠 (한두 줄 수정)

| 항목 | 문서 | 비고 |
|---|---|---|
| ⏸ 좌우 여백 16 → 32 · 헤더 로고 22 → 32 | [`docs/DESIGN_REVIEW.md`](./docs/DESIGN_REVIEW.md) §3 보류 두 줄 | `Screen`·`Header` 가 전 화면 공용이라 **한 번에** 본다. 🟢 순수 JS 라 OTA 로 나간다 |

### 문서가 코드보다 낡은 것 (되돌리기 사고 방지)

✅ **2026-09-11 오전에 여섯 자리를 닫았다.** 어제 밤 정리가 `CLAUDE.md` §16 만 고치고
나머지 요약표를 전부 놓쳤다. 다음 세션이 이 표들을 믿으면 vc2 가 안 올라간 줄 안다.

| 무엇이 낡아 있었나 | 상태 |
|---|---|
| `CLAUDE.md` §11 스택 표 — 앱·언어·라우팅·상태·FSRS·다국어가 전부 `❌ 미설치` | ✅ 고쳤다 |
| `CLAUDE.md` §16 머리 — 확정 결정 16건 · 미결정 7건 · `docs/` 12종 | ✅ 21건 · 6건 · 18종 |
| `docs/README.md` §2 구현 현황 · §3 검증 루틴이 `열둘` | ✅ 고쳤다(실제 14) |
| `docs/PLAN.md` 진행 현황표 — Phase 5·6·9 | ✅ 고쳤다 |
| `docs/STATS_SYSTEM.md` §0 — 구현이 전부 `❌` | ✅ 고쳤다 |
| `docs/KNOWLEDGE_SYSTEM.md` §0 — OCR·검색 | ✅ 고쳤다 |
| `common/PLAY_CONSOLE_STATUS.md` · `common/BUSINESS_INFO.md` 의 Re:Read 행이 vc1 에서 멈춤 | ✅ 고쳤다 |

🔴 **사각을 가드로 닫았다.** `check:docs` 가 5축에서 **8축**이 됐다(`CLAUDE.md` §16 요약 둘 + `verify` 개수).
변이 5종을 주입해 **전부 발화**하는 것을 확인했다(수 조작 셋 · 앵커 제거 · 가드를 빼고 문서는 그대로).
★ 한 사실이 두 문서에 적혀 있으면 대조 대상은 **둘 다**여야 한다. 한쪽만 재면 다른 쪽이 조용히 늙는다.

---

## 2. 오늘 끝낸 것 (2026-09-10)

- Phase 6 검색·통계 — `afd5fb6` (검증: 에뮬 화면 값 = 기기 DB 재계산 · 변이 20종 발화)
- Phase 9 실천 — `6eed6ed` (검증: v5 가 기기에서 돌았고 해제 후 재체크에도 `practice_logs` 1행)
- Phase 5 OCR — `5fbf007` (🔴 인식은 미판정)
- 책 select 와 최근 사용순 정렬 — `a95ef8c` (칩으로는 책을 만들 길이 없었다)
- vc2 빌드 — `c46a803` (14m 49s · 71.2MB · 업로드는 의도한 자리에서 막혔다)
- vc2 게시 — `ddc66a9` (21:28 · 권한 4 → 5 → 4)
- `android/` 정리와 백슬래시 사고 — `b501d4d`
- 하루 정리 — `713930e`

⚠ 🔴 **push 는 안 했다.** 로컬에만 **12건**이 있다(2026-09-11 실측 · 그전에 "9건"이라고 말한 것은 틀렸다).

### 🆕 2026-09-11 오전에 더한 것

- 문서 드리프트 여섯 자리 정리 + `check:docs` 를 8축으로 — `755811a` (검증: verify exit 0 · 변이 5종 발화)
- **결정 #22**(OCR 줄 선택 · 책담 승계) 문서화 + 순수 함수 + 가드 축 ⑥⑦⑧
  (검증: 변이 8종 전부 발화 · 그중 **책담 방식 그대로가 둘** 발화)

---

## 3. 대기 중

- 🙋 **사용자** — 폰의 vc1 삭제 승인(§0)
- 🙋 **사용자** — 상표 확인 "Re:Read"
- 🙋 **사용자 계정·키** — Phase 7(common_server 에 `reread` 등록 · 구글 OAuth 클라이언트 · Discord 웹훅) ·
  Phase 8(Supabase · Vercel · Anthropic API 키) · Phase 10(RevenueCat) · Phase 11(법무 문서 · 스토어 등록정보)
- ⏸ `common/DOC_SYSTEM.md` 수정 한 줄과 `common/WORKLIST_HANDOVER.md` 가 **커밋 안 된 채** 있다.
  그 문서를 만든 세션 몫이라 건드리지 않았다.
