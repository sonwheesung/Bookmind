# EDGE_CASES — 실제로 밟아 본 것과 그때 어떻게 되어야 하는가

> 각 시스템 문서의 "엣지 케이스" 절을 **v1 착수와 함께 여기로 모은다**
> (`KNOWLEDGE_SYSTEM.md` §8 · `REVIEW_SYSTEM.md` §8 이 그렇게 예고해 뒀다).
>
> 🔴 **이 문서에 있는 것은 전부 "실제로 해 본 것"이다.** 상상한 케이스는 각 시스템 문서에 남긴다 —
> 섞으면 "확인했다"와 "그래야 한다"가 구분되지 않는다.
>
> 신설: 2026-09-09 (Phase 2 CRUD 정밀 점검 결과를 담으며).

---

## 0. 표기

| | |
|---|---|
| ✅ | 문서대로 동작함을 **화면에서** 확인 |
| 🔧 | 결함이었고 **고쳤다** — 고친 날짜와 함께 |
| ⏸ | 알고 있으나 아직 안 고침(이유 명시) |
| 🚫 | 일부러 그렇게 둔다 |

---

## 1. 저장 (`KNOWLEDGE_SYSTEM.md` §1)

| 상황 | 기대 동작 | |
|---|---|---|
| 원문만 넣고 저장 | 성립한다. 책·페이지·생각·태그 전부 비어도 된다(기둥 1) | ✅ |
| 공백만 있는 원문 | 저장 버튼이 **잠긴다.** 빈 카드가 생기면 안 된다 | ✅ |
| 공백만 있는 생각·태그 | 빈 행을 만들지 않는다 | ✅ |
| 같은 문장을 두 번 저장 | **허용한다.** 🚫 중복 검사를 넣지 않는다 — 흐름을 끊는 것이 더 나쁘다 | 🚫 |
| 한 챕터를 붙여넣기(2,000자) | 저장된다. 목록은 3줄로 줄이고 상세에서 전문을 본다 | ✅ |
| 🔧 **긴 원문을 넣는 동안 저장 버튼이 화면 밖으로 밀린다** | 입력칸에 상한을 두고 그 안에서 스크롤한다. **저장 동선이 막히면 기둥 1 위반**이다 | 🔧 2026-09-09 |
| 🔧 **저장 버튼을 빠르게 두 번 누른다** | 한 건만 저장된다. 화면을 떠나므로 두 번째 탭이 들어올 틈이 있다 | 🔧 2026-09-09 |

## 2. 지식 카드 상세

| 상황 | 기대 동작 | |
|---|---|---|
| 🔴🔧 **[수정]에서 원문을 지우고 [저장]** | 저장 버튼이 **잠긴다.** 고치기 전에는 예외가 나고 사용자에게는 **아무 일도 안 일어난 것처럼** 보였다 — 릴리스에서는 조용한 먹통이 된다 | 🔧 2026-09-09 |
| [수정] 후 [취소] | 원래 내용으로 돌아온다 | ✅ |
| 🔧 **긴 생각이 여러 건** | 최신 하나만 펼치고 나머지는 접는다(§3). 안 접으면 삭제 버튼까지 스와이프 세 번이 걸렸다 | 🔧 2026-09-09 |
| 🔧 **생각 [떼기]** | 확인을 묻는다. 되돌릴 수 없는데 문장·책 삭제에만 확인이 있었다 | 🔧 2026-09-09 |
| 책 연결을 나중에 붙이기 | 언제든 붙이고 뗄 수 있다(§1.1) | ✅ |

## 3. 태그 (`KNOWLEDGE_SYSTEM.md` §5 · `DATABASE.md` §1.4)

| 상황 | 기대 동작 | |
|---|---|---|
| 같은 태그를 두 번 붙이기 | 중복 행이 생기지 않는다 | ✅ |
| `Habit` 과 `habit` | **같은 태그**로 본다(대소문자 무시) | ✅ |
| 뗐다가 **같은 이름을 다시** 붙이기 | 되살아난다. 🔴 새 행을 만들면 UNIQUE 위반으로 죽는다(§1.4) | ✅ |
| `a,,b , c ` 처럼 지저분한 입력 | 정확히 3개로 갈리고 빈 태그가 안 생긴다 | ✅ |
| 🔧 **상세에서 `alpha,beta` 입력** | 저장 화면과 **같은 규칙**으로 나눈다. 화면마다 규칙이 다르면 사용자는 두 번 배워야 한다 | 🔧 2026-09-09 |
| 🔧 **이미 붙어 있는 태그를 다시 추가** | 아무 일도 안 일어나는 대신 **그렇다고 말해 준다.** 조용하면 "안 눌렸나"로 읽힌다 | 🔧 2026-09-09 |
| 🔧 **태그 이름이 아주 길다** | 이름만 줄어들고 **제거 표식(×)은 남는다.** 라벨에 붙여 쓰면 × 까지 잘려 "못 지우는 태그"가 된다 | 🔧 2026-09-09 |

## 4. 삭제 규칙 (`DATABASE.md` §3)

| 상황 | 기대 동작 | |
|---|---|---|
| 🔴 **책을 지운다** | 지식은 **남고** `book_id` 만 끊긴다. 확인 문구가 **남는 문장 수**를 정확히 말한다 | ✅ |
| 지식을 지운다 | 목록·홈 카운트에서 사라진다. 실천은 남고 복습 로그도 남는다 | ✅ |
| 지식을 지운 뒤 태그 | 그 지식만 쓰던 태그는 정리되고, **다른 지식이 쓰는 태그는 남는다** | ✅ |
| 지운 지식이 복습 큐에 | 나타나지 않는다. 두 겹으로 막고 가드가 매번 잰다 | ✅ (가드) |

## 5. 다국어 (`I18N_SYSTEM.md`)

| 상황 | 기대 동작 | |
|---|---|---|
| 앱 언어를 바꾼다 | 전 화면 + **시스템 대화상자 문구까지** 바뀐다 | ✅ |
| 기기 언어와 앱 설정이 다르다 | **앱 설정이 이긴다.** 껐다 켜도 유지된다 | ✅ |
| 저장한 원문·내 생각 | **바뀌지 않는다.** 쓴 언어 그대로다(§3 축 분리) | ✅ |

## 5.1 🔧 죽은 키 — 화면을 걷어냈는데 문구가 남았다 (2026-09-09)

| | |
|---|---|
| **증상** | 화면은 멀쩡하다. `home.today.reviewCount` · `knowledge.tags.empty` 가 두 언어에 그대로 남아 **아무도 안 부르는 문구**가 됐다 |
| **원인** | 디자인 패스에서 홈의 카운트 문구를 바꾸고(`count` 로 이름이 바뀜) 태그 빈 상태를 지우면서, 화면만 고치고 리소스를 안 지웠다 |
| **왜 안 걸렸나** | 🔴 `check:i18n` 의 ①②는 **언어끼리만** 대조한다. 두 파일에 **대칭으로** 남아 있으면 차이가 0 이다. ⑤(없는 키)는 *부르는데 없는* 경우만 보므로 반대 방향은 못 본다 |
| **사각 닫기** | 축 **⑥ 죽은 키** 신설(`PLANNED` 예외는 이유 필수 · 예외가 낡으면 그것도 FAIL). 변이 확인: 죽은 키 심기 → FAIL · `PLANNED` 키 삭제 → *"예외 낡음"* FAIL |
| **왜 중요한가** | 화면은 안 깨지므로 **영원히 안 들킨다.** 대신 언어를 15개로 늘리는 날 죽은 키 하나가 **번역 15건**이 된다 |

★ 같은 날 반대 방향(양쪽에서 **지워서** 화면이 키 문자열을 뱉을 뻔한 것)도 겪었다 — 축 ⑤ 가 그것이다.
**두 파일을 대칭으로 틀리면 대조 검사는 침묵한다**(`I18N_SYSTEM.md` §5.2).

## 7. 🔧 소스에 NUL(0x00)이 박혔다 — 기능은 멀쩡했다 (2026-09-09)

| | |
|---|---|
| **증상** | 없다. `features/backup/merge.ts` 의 열쇠 구분자가 공백이 아니라 **NUL 문자**였는데 병합이 정상 동작했다 |
| **원인** | 파일을 쓸 때 구분자 한 글자가 `0x00` 으로 들어갔다. 열쇠를 잇는 데만 쓰이므로 **어떤 값이든 결과가 같다** |
| **왜 안 걸렸나** | 🔴 `tsc`·`lint`·`check:backup`(왕복 · 합치기 · 거부 전부) 이 **모두 초록이었다.** 동작이 안 바뀌니 동작을 재는 검사로는 영원히 안 잡힌다 |
| **잡은 것** | `check:chars` — 바이트로 읽는 유일한 가드다. 형제가 정규식에 `0x08` 이 박혀 19일간 죽어 있던 사고(`I18N_SYSTEM.md` §5.1)와 **같은 종류**다 |
| **수정** | `const SEP = '\\u0000'` 로 **이스케이프를 소스에 적는다.** 값은 그대로고 소스는 ASCII 로 남는다 |

★ 이 항목의 값은 수정이 아니라 **"기능 테스트가 전부 초록이어도 소스는 오염될 수 있다"** 는 사실이다.
그래서 `check:chars` 는 다른 가드가 아무리 늘어도 빠지지 않는다.

## 7.2 ✅ 비행기 모드 복습 (2026-09-09)

| 상황 | 기대 동작 | |
|---|---|---|
| 비행기 모드에서 오늘의 복습 | 큐 → 단서 → 원문 공개 → 4등급 → 저장까지 **전부 돈다.** 이 앱은 복습에 네트워크를 쓰지 않는다(결정 #4) | ✅ |
| 그때 남은 것 | `review_logs` 에 `good` 한 줄(비행기 모드 시각) · `review_schedules` 가 `learning_steps=1 · reps=2` 로 전진 | ✅ (DB 직접 조회) |
| 등급을 누른 뒤 홈 | 다시 *"문장 1개"* 로 보인다 — 🚫 버그가 아니다. FSRS 학습 단계가 **10분 뒤**라 오늘 안에 또 걸리고, 큐 판정은 날 단위다(`REVIEW_SYSTEM.md` §2.1·§2.3) | 🚫 |

⚠ **Expo Go 라서 앱을 다시 띄우려면 Metro 가 필요하다.** 그래서 "먼저 띄우고 → 비행기 모드"로 쟀다.
**콜드 스타트가 오프라인에서 되는지는 아직 모른다** — 그건 릴리스 빌드의 축이다(Phase 11).

## 8. 백업 (`BACKUP_SYSTEM.md`)

| 상황 | 기대 동작 | |
|---|---|---|
| 내보내기 | 공유 시트에 `ReRead-backup-YYYYMMDD-HHmm.json` 하나. 캐시 임시 파일은 지운다 | ✅ 2026-09-09 |
| 시트를 닫는다 | *"마지막 내보내기: 날짜"* 로 바뀐다. 🚫 *"백업됨"* 이라고 쓰지 않는다(OS 가 성공/취소를 안 알려준다) | ✅ |
| 🔴🔧 **미리보기가 tombstone 을 셌다** | *"문장 2"* 라고 안내하고 화면에는 하나만 나타났다. 파일에는 지운 행도 실리기 때문 → **살아 있는 행만** 센다 | 🔧 2026-09-09 |
| 지운 문장이 든 파일을 가져온다 | 지워진 채로 들어온다. 🔴 **되살아나지 않는다** | ✅ |
| 같은 파일을 두 번 가져온다 | *"추가 0 · 갱신 0 · 건너뜀 5"* — 두 번째는 아무것도 안 바뀐다 | ✅ |
| 모르는 컬럼(`future_col`)이 섞인 파일 | 그 컬럼만 버리고 나머지를 넣는다 | ✅ |
| 백업 파일이 아닌 JSON · 미래 버전 파일 | 거부한다. 기기 내용은 그대로 | ✅ (가드) |
| 같은 이름의 태그 · 같은 날의 실천 로그 | UNIQUE 로 터지지 않는다 — 재매핑·건너뛰기 | ✅ (가드) |
| ⏸ 20MB 초과 파일 | 거부한다 | 가드로만 확인(기기에서 안 만들어 봤다) |

⚠ **문서 피커가 "최근" 탭에 이미지를 먼저 보여준다**(`*/*` 이라서). 🚫 `application/json` 으로 좁히지
않는다 — 제공자가 `.json` 을 `octet-stream` 으로 라벨하면 파일이 **회색으로 죽어** 이유도 모른 채 막힌다.

## 7.1 🔴 미리보기 숫자가 왜 사각이었나

파일에 tombstone 을 싣기로 한 것(§1)과 미리보기 숫자를 `배열 길이`로 잡은 것이 **각각은 맞았다.**
둘이 만나는 자리에서만 틀렸고, 그 자리를 재는 검사가 없었다 → 가드에 축(미리보기 수)을 하나 더 세웠다.
★ **"둘 다 맞는데 만나면 틀리는 곳"** 은 단위 검사로는 안 나온다. 화면에서 한 번 보는 값이 여기 있다.

## 9. 알림 (`REVIEW_SYSTEM.md` §6)

| 상황 | 기대 동작 | |
|---|---|---|
| 설정에서 켠다 | 권한을 묻고, 허용하면 앞으로 7일치가 하루 1건씩 예약된다 | ✅ 2026-09-09 |
| 권한을 거절한다 | 스위치가 도로 꺼지고 안내 문구가 바뀐다. 🚫 켜진 척하지 않는다 | ✅ (코드) |
| 시각이 되면 | *"오늘의 복습 / 다시 만날 문장이 있습니다."* 가 뜬다 | ✅ (시계를 넘겨 확인) |
| 끈다 | 예약이 **0건**이 된다 | ✅ (`dumpsys alarm`) |
| 복습할 것이 없는 날 | 예약 자체를 안 한다 | ✅ (가드) |
| 🔴🔧 **켜자마자 예약이 두 배** | 7일치가 14건이었다. 스위치 핸들러와 effect 가 **동시에** 돌아 둘 다 "취소할 게 없다"를 봤다 | 🔧 2026-09-09 |
| ⏸ 아이콘 | Expo Go 의 아이콘으로 뜬다. 우리 아이콘은 `prebuild` 뒤에나 확인된다(§6.3) | ⏸ |
| ⏸ Doze · 강제 종료 · 재부팅 | 에뮬레이터가 재현하지 않는다. **실기기 다일차**가 완료 기준이다 | ⏸ |

### 9.1 🔴 왜 이 결함은 화면으로 못 잡나

알림은 **안 오는 것도, 두 번 오는 것도 사용자가 말해 주지 않는다.** 앱 화면에는 아무 흔적이 없다.
그래서 이 축의 검증은 **기기 상태를 직접 읽는 것**(`dumpsys alarm` · `dumpsys notification`)이고,
그건 스크린샷보다 값이 크다. 백업의 `sqlite3` 조회와 같은 성격이다(§7.2·§8).

★ 이번 세션에서 **화면이 멀쩡한데 틀린 것**을 세 번 만났다:
죽은 i18n 키(§5.1) · 소스의 NUL(§7) · 알림 이중 예약(§9). 셋 다 잡은 것은 **바이트·기기 상태를 직접 읽는 검사**였다.

## 10. 🔧 순환 import — 실기기에 처음 올린 날 번들이 알려줬다 (2026-09-09)

| | |
|---|---|
| **증상** | 없다. 앱은 정상 동작했다 |
| **원인** | `knowledge/repo → review/repo`(`scheduleNewCard`)와 `review/repo → knowledge/repo`(`thoughtsOf`)로 고리가 닫혀 있었다 |
| **왜 안 걸렸나** | 🔴 `tsc`·`lint`·가드 7종이 **전부 초록**이었다. 순환 자체는 타입 오류가 아니고, 지금은 둘 다 **호출 시점에** 함수를 찾아 안 깨진다 |
| **누가 잡았나** | **Metro** 가 번들할 때마다 경고하고 있었다. 에뮬레이터로 볼 땐 로그를 안 읽었고, 폰에 처음 올리며 번들 로그를 보다가 눈에 들어왔다 |
| **위험** | 어느 한쪽이 모듈 **최상위**에서 상대를 쓰는 순간 `undefined` 가 된다. 증상은 "가끔 빈 화면"이라 원인을 못 찾는다 |
| **수정** | `features/knowledge/thoughts.ts` 로 갈라 양쪽이 그것을 보게 했다. 🚫 경고를 끄지 않았다 |
| **재검증** | 폰에서 다시 로드해 **Require cycle 경고 0건** 확인 |

★ 오늘 넷째다. **화면은 멀쩡한데 틀린 것** — 죽은 i18n 키(§5.1) · 소스 NUL(§7) · 알림 이중 예약(§9) · 순환 import(§10).
넷 다 동작을 재는 검사는 놓쳤고, **다른 층을 읽는 것**(바이트 · 기기 상태 · 번들 로그)이 잡았다.

## 11. 🔴🔧 키보드가 입력창을 가렸다 — 감싸 놓고 아무 일도 안 하고 있었다 (2026-09-09)

| | |
|---|---|
| **증상** | 화면 아래쪽 입력창을 누르면 키보드가 그것을 덮는다. 대표님 폰에서 발견 |
| **원인** | `Screen` 이 `KeyboardAvoidingView` 로 감싸면서 **안드로이드에 `behavior={undefined}`** 를 줬다. 그 값이면 KAV 는 **아무 일도 하지 않는다** |
| **왜 안 걸렸나** | 🔴 `tsc`·`lint`·가드 7종 전부 초록. 코드에 `KeyboardAvoidingView` 라는 이름이 **보이니까** 처리된 줄 알았다. 에뮬레이터에서도 안 밟았다 — 마우스로 누르면 키보드가 안 뜬다 |
| **수정** | 조각(diary) → Idea Repository 로 이어진 방식을 승계. 겹침을 **재서** 스크롤 영역을 줄인다 |

### 11.1 왜 "여백 추가" 가 아니라 "영역 축소" 인가

여백을 더하면 **손으로 스크롤해야** 입력창에 닿는다. 영역 자체가 짧아지면 안드로이드 `ScrollView` 가
크기 변화에 맞춰 **포커스된 입력창을 스스로 보이는 데까지 올린다.**

```
겹침 = (Screen 하단 좌표) - (키보드 윗변 좌표)
```

🟢 창이 이미 줄어드는 기기(`adjustResize` 가 먹는 경우)에서는 겹침이 0 이라 아무 일도 안 한다.
**기기를 코드로 나누지 않고 실제로 재서** 판단한다.

⚠ 승계한 코드에 실기기에서 다듬은 흔적이 셋 있다.
- **두 번 잰다**(60ms · 350ms). Gboard 는 뜬 뒤 제안 줄이 붙어 높이가 한 번 더 커진다
- iOS 는 `automaticallyAdjustKeyboardInsets` 가 같은 일을 해서 **안드로이드만** 줄인다(안 그러면 두 번 밀린다)
- 그래도 안 올라오면 포커스된 입력창을 찾아 직접 스크롤하는 보완책

### 11.2 형제 앱 조사 (2026-09-09)

| 앱 | 키보드 처리 |
|---|---|
| 조각(diary) | 🟢 **원본** — `Screen` + `useKeyboard` |
| Idea Repository | 🟢 승계. 주석: *"2026-08-17 실기기: 태그 입력이 키보드에 가려지는 문제로 도입"* |
| LinkMemo | ❌ `Screen` 은 있는데 키보드 처리가 없다 |
| My Word | ❌ 없다 |

★ **우리 것이 가장 나빴다.** 없는 것보다 **있는 척하는 것**이 나쁘다 — 다음 사람이 "처리돼 있네" 하고 지나간다.

## 12. 🔴🔧 연속일 계산이 틀리면 **멈춘다** — 변이 주입이 찾았다 (2026-09-10)

`check:stats` 를 만들고 변이 10종을 넣어 보다가, 다섯 번째 변이에서 가드가 **실패하지 않고 멎었다.**
30분을 기다려도 아무것도 안 나왔다.

**증상** · `npm run check:stats` 가 출력 없이 영원히 돈다. 실패도 성공도 아니다.

**원인** · 넣은 변이는 `previousDayKey` 에서 `d - 1` 을 `d` 로 바꾼 것 하나다(어제 대신 오늘을 돌려준다).
그러면 `streakDays` 의 `while (days.has(cursor))` 에서 커서가 **줄지 않는다.**

```
cursor = '2026-09-10'  →  days 에 있다  →  n += 1  →  previousDayKey → '2026-09-10'  →  또 있다 → …
```

🔴 **이건 가드의 문제가 아니라 앱의 문제다.** 같은 일이 실기기에서 일어나면 통계 화면이
느려지는 게 아니라 **JS 스레드가 멈춘다.** 화면 전체가 얼고, 사용자에게는 앱이 죽은 것으로 보인다.
숫자가 하나 틀리는 것과 앱이 서는 것은 등급이 다르다.

**수정** · `streakDays` 가 커서가 **반드시 줄어드는지** 매 회 확인하고, 안 줄면 던진다.
날짜 키가 `YYYY-MM-DD` 라 문자열 비교로 충분하다. 잘못된 숫자는 고칠 수 있지만 멈춘 화면은 못 고친다.

**🔴 이전 검사가 왜 못 잡았나** · 셋 다 못 잡을 수밖에 없었다.

| 층 | 왜 침묵했나 |
|---|---|
| `tsc` · `lint` | 타입도 문법도 멀쩡하다. 무한 반복은 타입이 아니다 |
| SELF-TEST | 순서상 `streakDays` 를 **먼저** 부른다. 거기서 멎어서 뒤의 `previousDayKey` 검사에 도달을 못 했다 |
| 변이 주입 자체 | 발화 여부만 봤다. 🔴 **"멈춤"을 실패로 세는 자리가 없었다** |

**사각 닫기** · 변이 주입 스크립트에 `timeout=180` 을 걸어 멈춘 변이를 **침묵이 아니라 사고**로 본다.
그리고 종료 보장을 코드에 박아 두어, 다음에 같은 실수를 해도 화면이 서는 대신 예외가 난다.

★ **이번에도 "화면은 멀쩡한데 틀린 것"의 변주다.** 다만 이번 것은 화면이 멀쩡하지도 않다.
`CLAUDE.md` §16 의 다섯 건보다 한 칸 더 나쁜 자리이고, 찾은 것은 테스트가 아니라 **변이 주입**이다.

---

## 13. 🔧 백슬래시가 세 겹을 지나며 문서를 깨뜨렸다 (2026-09-10)

**증상** · `check:chars FAIL (1개 파일)` — `CLAUDE.md` offset 56732 에 `0x08`.

**원인** · 문서를 고치는 파이썬 스크립트에 Windows 경로를 **일반 문자열**로 넣었다.
그런데 그 스크립트는 도구 호출 → 셸 → 파이썬 **세 겹**을 지나고, 겹마다 백슬래시가 한 개씩 벗겨진다.
마지막에 파이썬이 본 것은 `\b` 였고, 파이썬은 그것을 **백스페이스(0x08)** 로 읽었다.

```
내가 쓴 것        \\\\builds      (도구 호출 안)
셸이 받은 것       \\builds      (한 겹 벗겨짐)
파이썬이 읽은 것   \builds      (또 한 겹)
파일에 들어간 것   <0x08>uilds     (파이썬이 \b 를 백스페이스로 읽었다)
```

🔴 **제어문자만 문제가 아니다. 경로 자체가 거짓이 됐다.** `D:` 다음이 `uilds` 가 되고
`\r` 자리에서는 줄까지 갈라졌다. 다음 사람이 그 경로로 파일을 찾으면 못 찾는다.

**수정** · 백슬래시가 든 글을 **문자열 리터럴로 나르지 않는다.**
따옴표 친 heredoc(`<<'EOF'`)으로 파일에 먼저 쓰고, 파이썬은 그 파일을 **읽어서** 끼워 넣는다.
heredoc 은 아무것도 해석하지 않으므로 겹이 하나도 없다.

🔴 그리고 쓰기 **전에** 제어문자를 확인한다. 이 사고는 두 번 났고, 두 번째는 이 문단을 쓰다가 났다.

**🔴 이전 검사가 왜 못 잡았나** — 못 잡은 게 아니라 **안 돌렸다.**

| 층 | 왜 침묵했나 |
|---|---|
| 사람 눈 | `0x08` 은 화면에 아무것도 안 그린다. 마크다운 렌더에서도 안 보인다 |
| `check:docs` | 커밋 전에 이것만 돌렸다. 문서의 **개수**를 세지 바이트를 안 읽는다 |
| `check:chars` | 🔴 **바이트를 읽는 유일한 가드인데 안 돌렸다** |

★ **`npm run verify` 를 건너뛰고 가드 하나만 골라 돌린 것이 사고의 원인이다.**
`verify` 가 열넷을 순서대로 도는 이유가 이것이다. 어느 것이 잡을지 미리 알 수 없다.
🔴 **커밋 전에는 `verify` 하나만 돌린다.** 급할수록 그렇다.

⚠ 오늘 `check:chars` 가 잡은 **두 번째** 건이다. 첫 번째는 소스의 NUL(0x00)이었다(§7).
둘 다 "화면은 멀쩡한데 바이트가 틀린" 모양이고, 다른 가드는 원리상 못 본다.

---

## 14. 🔧 홈에 `42p쪽` 이 떴다 — 자유 텍스트에 접사를 무조건 붙이고 있었다 (2026-09-11)

**증상** · 실기기 홈의 최근 저장 출처 줄이 `명상록 · 마르쿠스 아우렐리우스 · **42p쪽**` 이었다.

**원인** · `app/index.tsx` 가 `t('knowledge.pageShort', { page: k.page })` 로 **원문 값에 바로** 접사를 붙였다.
`DATABASE.md` §2 는 `page` 를 **자유 텍스트**로 정해 뒀다(`"123p"` · `"3장"`).

```
입력    ko          en
42   →  42쪽  ✅    p.42   ✅
42p  →  42p쪽 🔴    p.42p  🔴
3장  →  3장쪽 🔴    p.3장  🔴
```

⚠ **영어는 접두사라 "안 붙이면 된다"로 못 고친다.** `42` 는 `p.42` 여야 한다.
→ **숫자일 때만 접사를 씌우고, 아니면 사용자가 쓴 그대로** 둔다(`features/knowledge/compute.ts`).
🚫 입력을 숫자로 막는 쪽으로 고치지 않았다. `3장` 을 쓰게 두는 것이 설계 의도이고
(동양 고전은 쪽이 아니라 장으로 센다), 막으면 저장 흐름에 규칙이 하나 는다(기둥 1).

**🔴 이전 검사가 왜 못 잡았나**

| 층 | 왜 침묵했나 |
|---|---|
| `tsc` · `lint` | 타입도 문법도 멀쩡하다. `string` 에 `string` 을 붙이는 코드다 |
| `check:i18n` | 키는 양쪽에 다 있고 보간 변수도 같다. **문구가 맞는지는 안 본다** |
| 가드 열넷 | 🔴 **지식 카드 표시를 재는 가드가 아예 없었다.** `KNOWLEDGE_SYSTEM.md` 는 가장 큰 문서인데 검색·책·OCR 만 가드가 있었다 |
| 에뮬 실측 | 🔴 그때 넣은 페이지 값이 **전부 숫자**였다. 숫자만 넣으면 이 결함은 **영원히 안 보인다** |

★ **또 그 형태다** — *"검사가 재려는 것이 입력에서 이미 참이면 그 검사는 아무것도 안 지킨다."*
`100%` 이스케이프 · `weekCells` 월요일 · OCR 읽기 순서에 이어 **네 번째**다.
이번에는 검사가 아니라 **시드 데이터**가 그랬다. 내가 `第 1 章` 과 `42p` 를 섞어 넣어서 드러났다.

**사각 닫기** · `npm run check:knowledge` 신설(4축 · SELF-TEST). 축 ④ 가 **화면이 판정을 거치는지**를
소스에서 재고, 옛 코드 모양을 정규식으로 막는다(양성 대조 포함).

⚠ **이 수정은 아직 폰에 없다.** JS 라 OTA 로 나가는데, 🔴 발행은 사용자가 지시할 때만 한다(`OTA_SYSTEM.md` §7).

---

## 15. 🔴 `adb shell input text` 로 한글을 못 넣는다 (2026-09-11 · 갤럭시 S24)

폰에 시드 데이터를 넣으려고 화면을 직접 운전하다가 막혔다.

```
input text "Habit123"      →  ✅ 그대로 들어간다
input text "%s습관은…"      →  🔴 java.lang.NullPointerException:
                                 Attempt to get length of null array
```

⚠ **조각 세션이 적어 둔 것과 실패 모양이 다르다.** `common/EMULATOR_POOL.md` §2.2 는
*"한글 IME 가 켜져 있으면 글자를 **바꿔** 넣는다"*(`AAAAA` → `ㅁㅁㅁㅁㅁ`)라고 적어 뒀는데,
이 폰에서는 **바뀌는 게 아니라 던지고 끝난다.** 아무것도 안 들어간다.

🟢 **던지는 쪽이 차라리 낫다.** 바뀌어 들어가면 `uiautomator` 로 확인하기 전까지 모르고,
그 사이에 틀린 내용이 저장된다. 예외는 그 자리에서 멈춘다.

→ **그래서 한글 본문은 UI 로 넣지 않는다.** 우리 **백업 가져오기**로 넣는다
(`BACKUP_SYSTEM.md` §4). 텍스트가 IME 를 아예 안 지나가고, 덤으로 **가져오기를 실기기에서 처음 밟았다**
(그전까지 에뮬 실측뿐이었다 · 같은 문서 §9).

⚠ `adb push` 할 때 Git Bash 가 `/sdcard/...` 를 `C:/Program Files/Git/sdcard/...` 로 바꾼다.
`MSYS_NO_PATHCONV=1` 을 붙인다.

---

## 16. 🔴🔧 독서 앱이 마이크를 요구하고 있었다 (2026-09-11)

### 증상

Play 비공개 테스트를 준비하며 vc2 AAB 의 매니페스트를 열어 봤더니
`android.permission.RECORD_AUDIO` 와 `android.permission.SYSTEM_ALERT_WINDOW` 가 들어 있었다.
둘 다 우리가 요청한 적이 없다. **스토어 페이지의 앱 권한 목록은 사용자가 설치 전에 본다.**

### 원인

`SYSTEM_ALERT_WINDOW` 는 Expo bare 템플릿의 기본 매니페스트에서 왔다.
그 템플릿은 자기 파일에 이렇게 적어 두고 있었다.

```xml
<!-- OPTIONAL PERMISSIONS, REMOVE WHATEVER YOU DO NOT NEED -->
```

**지우라고 적혀 있는 것을 안 지운 것이다.** `RECORD_AUDIO` 는 우리 코드와 플러그인,
`node_modules` 의 어느 매니페스트에도 없어 원격 AAR 에서 온 것으로 보이나 출처는 확인하지 않았다.

### 🔴 왜 기존 검증이 못 잡았나

**잴 대상이 아예 목록에 없었다.** 가드 15종은 전부 **소스**를 본다.
`app.json` 의 `android.permissions` 는 `undefined` 였으므로, 설정 파일을 재는 검사를 만들었다면
*"우리는 아무 권한도 요청하지 않는다"* 로 읽고 초록이었을 것이다.
**실제 권한은 30개였다.** 라이브러리 매니페스트와 템플릿이 병합된 결과는 **빌드 산출물에만 있다.**

★ §16 의 "화면은 멀쩡한데 틀린 것" 계보에 한 칸 더 붙는다. 이번 것은 화면에도 안 보인다.
**스토어 페이지에서만 보이고, 그때는 이미 사용자가 보고 있다.**

### 사각 닫기

`scripts/check-aab-permissions.mjs` 를 만들었다. 의존성 없이 ZIP 을 풀고
aapt2 protobuf 매니페스트를 파싱해 **권한 집합을 허용 목록과 대조**한다.
허용 목록 밖은 전부 실패다. 금지 목록만 두면 **모르는 권한이 새로 들어오는 것**을 못 잡는다.

### 🔴🔧 그 가드의 자체 검사도 같은 병에 걸려 있었다

`receiver` 의 `android:permission="…DUMP"` 를 권한 요청으로 세면 안 된다.
그걸 재는 양성 대조를 넣었는데, **`receiver` 요소에 달아 놨다.**
요소 이름 검사가 이미 그것을 걸러내므로 **속성 이름 검사는 한 번도 측정되지 않았다.**
`str(a, 2) === 'name'` 을 통째로 없애는 변이를 넣어도 자체 검사는 초록이었다.

→ `name` 이 아닌 속성을 **`uses-permission` 안에** 함께 두는 것으로 고쳤다. 그제서야 발화했다.

★ **다섯 번째다.** `100%` 이스케이프 · `weekCells` 월요일 · OCR 읽기 순서 · `42p쪽` 페이지 접사,
그리고 이번. **검사가 재려는 것이 입력에서 이미 참이면 그 검사는 아무것도 안 지킨다.**
이번엔 그 문장을 알고 있는 채로 또 밟았다. 아는 것과 매번 확인하는 것은 다르다.

---

## 6. 아직 안 밟은 것

| | 왜 |
|---|---|
| OCR · 실천 화면 | 아직 없다(Phase 5·9) |
| 알림의 실기기 축 | Doze·강제 종료·재부팅·아이콘 — Expo Go 로는 원리상 못 닫는다(§9) |
| ~~책 제목·저자 **수정**~~ | ✅ **2026-09-09 만들었다.** 제목이 비면 저장이 잠긴다 — §2 의 그 결함을 처음부터 안 만들었다 |
| 성능 정량 | 2,000자 카드에서 눈에 띄는 지연은 없었으나 **재지는 않았다** |
| 실기기 | 에뮬레이터만 봤다. Doze·배터리 최적화는 재현되지 않는다(Phase 4 완료 기준) |

---

*최종 갱신: 2026-09-10 — §13(백슬래시가 문서를 깨뜨렸다) · §12(연속일 무한 반복).
그 앞은 2026-09-09 — 문구 검수 반영 중 죽은 키 2개를 찾아 §5.1 로 남기고 가드 축 ⑥ 을 세웠다.
그 앞은 Phase 2 CRUD 정밀 점검(서브에이전트 170회 조작 · 스크린샷 근거)에서 결함 7건을 찾아 그날 전부 고쳤다.
그중 하나(🔴 원문을 지우고 저장)는 릴리스에서 조용한 먹통이 될 것이었다.*
