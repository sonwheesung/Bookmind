# MONETIZATION_SYSTEM — 광고 + 광고 제거 (🔄 2026-09-21 · ~~구독 (Premium)~~)

> 🔄 **2026-09-21 광고 + 광고 제거**(결정 #29). AI 를 빼면서(결정 #28) Premium 의 중심이 사라져 구독 모델을 접었다.
> 모든 기능은 누구에게나 열리고, 광고 제거는 **광고가 없는 화면**을 산다. 정책 정본은 `../CLAUDE.md` §7.
> 🔄 **결정 #31**: **무료 한도 없음 · 광고 제거는 평생 일회성**(`expo-iap` · 비소비성 `remove_ads` · 서버 없음) ·
> **배치는 단어 앱 My Word 와 같게, 복습 시작 전 전면만 뺀다**(배너 = 홈 · 문장 목록 · 책 목록 · 복습 끝 / 전면 = 복습 끝 한 번). 가격만 ⚠ 미결정 B.
> My Word 에 없는 광고 동의(UMP)와 연령 미달 맞춤 광고 끄기는 더한다.
> 🔴 넣기 전에 할 일(AdMob · `app-ads.txt` · UMP 동의 · 연령 미달 맞춤 광고 끄기 · 처리방침 · Play 데이터 보안 · 새 AAB)은 `../CLAUDE.md` §7 · `PLAN.md` Phase 10.
> **아래 §0 이하의 구독 설계는 기록이다.** ~~구독 단독. 광고 없음(결정 #10)~~.
> 사용자가 돈을 내는 이유는 저장이 아니라 **"계속 기억하게 해주고 행동까지 연결해주기 때문"** 이다(기획서 §28).
> 승계: ~~형제 앱의 RevenueCat + common_server 엔타이틀먼트 조합~~ → 🔄 단어 앱 My Word 의 광고 · 결제 코드(결정 #31).

---

## A. 🔴 광고 · 광고 제거 구현 (2026-09-21 · 결정 #29 · #31)

사용자 *"전부 다 진행해줘"*. 원본은 My Word(`C:\project\my_word\My_word`)의 `src/components/AdBanner.tsx` ·
`src/hooks/useInterstitialAd.ts` · `src/contexts/PurchaseContext.tsx` 이고, 같은 Expo SDK 54 · RN 0.81.5 라 그대로 옮긴다.
구조만 우리 방식(zustand 저장소 · 순수 판정은 `compute` 파일 · 가드)으로 바꾼다.

### A.1 부팅 흐름

```
부팅 → 연령 확인(결정 #25)이 끝나기를 기다린다(통과 · 미달 · 닫음 모두 끝이다)
     → 맞춤 광고 판정  decideAdRequest(통과 기록 · 미달 기록)          ← 순수 · 가드가 잰다
     → 광고 동의 UMP   requestInfoUpdate → 필요하면 폼(EEA · 영국)     ← 미달이면 미성년 표시로 폼을 안 띄운다
     → setRequestConfiguration → initialize → ready
     → 배너 · 전면은 ready 뒤에만 광고를 요청한다
광고 제거: 부팅 때 캐시를 먼저 읽고 → Play 에 다시 묻고(getAvailablePurchases) → 캐시를 갱신한다
```

- 🔴 **연령 확인보다 먼저 광고를 요청하지 않는다.** 미달인 사람에게 맞춤 광고가 한 번이라도 나가면 되돌릴 수 없다.
- 🔴 **광고 제거를 산 사람은 SDK 초기화도, 광고 요청도 하지 않는다**(My Word 규칙 · 보이지 않을 광고를 부르지 않는다).
- 광고 모듈이 없는 환경(Expo Go)에서는 조용히 아무것도 안 한다. 앱은 그대로 돈다.

### A.2 맞춤 광고 판정 (`features/ads/compute.ts`)

| 연령 기록 | 맞춤 광고 | 미성년 표시(`tagForUnderAgeOfConsent`) |
|---|---|---|
| 통과(유효) | 허용(동의가 있을 때) | 아니오 |
| 미달(유예 안) | 🔴 끈다 | 예 |
| 없음(닫았음 · 못 읽음) | 🔴 끈다 | 아니오 |

🔴 **모르면 끈다.** 통과 기록이 있을 때만 맞춤 광고가 열린다. 비맞춤 광고는 광고 수익이 줄지만 틀릴 수 없는 쪽이다.

### A.3 배치 (`features/ads/compute.ts` 의 `AD_PLACEMENTS` · 가드가 화면 파일과 대조)

| 종류 | 화면 | 조건 |
|---|---|---|
| 하단 고정 배너(적응형) | 홈 `app/(tabs)/index.tsx` · 문장 `app/(tabs)/knowledge.tsx` · 책 `app/(tabs)/books.tsx` · 복습 끝 `app/review.tsx` | `Screen` 의 `footer` 자리(스크롤 밖) · 탭 화면에서는 탭 바 바로 위 |
| 전면 | 복습 끝 `app/review.tsx` | 끝 화면에 들어온 뒤 **500ms** · **한 번** · 🔴 이번 복습에서 **한 장 이상 넘겼을 때만**(빈 큐로 들어와 본 끝 화면은 복습을 마친 게 아니다) |
| 🚫 없음 | 문장 저장 · 수정 · 복습 진행 중 · 설정 · 백업 · 연령 확인 · 그 밖의 모든 화면 | 가드가 이 파일들에 광고 부품이 없는지 잰다 |

🚫 복습 **시작 전** 전면은 없다(결정 #31 · 기둥 7).

### A.4 광고 단위 ID (`features/ads/config.ts`)

- 개발 빌드(`__DEV__`)는 늘 Google 테스트 ID 를 쓴다.
- ✅ **2026-09-21 AdMob 에 Re:Read 앱과 광고 단위 둘을 만들었다**(§A.6.1 의 ID). ~~만들기 전에는 릴리스도 테스트 ID 다~~. 비공개 테스트 동안은 스위치가 꺼져 있어 어느 ID 로도 요청하지 않는다.
- `app.json` 의 `androidAppId` 도 같은 쌍이어야 한다. 🔴 가드가 **앱 ID 와 단위 ID 가 둘 다 테스트이거나 둘 다 실제**인지 잰다. 섞이면 광고가 안 나오거나 무효 트래픽이 된다.
- 실제 ID 를 받으면 `config.ts` 와 `app.json` 두 곳을 한 커밋에 바꾼다.

### A.5 광고 제거 (`features/purchase/`)

- 상품: 비소비성 `remove_ads` · `expo-iap` 로 Play 결제 직접. 🚫 서버 · RevenueCat · 영수증 서버 검증 없음(일회성이라 뚫려도 손해가 상품 한 건이다 · My Word 판단 승계).
- 🔴 **진실은 Play 다.** 기기 캐시(AsyncStorage `reread-ad-free`)는 부팅 직후 광고가 번쩍이지 않게 하는 용도다. 매 부팅 다시 확인하고, 환불되면 목록에서 빠지므로 다음 부팅에 광고가 돌아온다.
- 🔴 **Play 조회가 실패하면 캐시를 그대로 둔다.** 오프라인에서 산 사람에게 광고를 되돌려주지 않는다.
- 🔴 **`finishTransaction({ isConsumable: false })` 를 반드시 부른다.** 안 부르면 Play 가 3일 뒤 자동 환불한다.
- 결제 대기(`pending`)는 아직 지급하지 않는다. 판정은 순수 함수 `ownsRemoveAds` 하나다(가드가 잰다).
- 캐시 키는 백업 파일에 안 들어간다(백업은 DB 만 옮긴다). 기기를 바꾸면 `구매 복원` 으로 되찾는다.

### A.6 설정 화면

```
광고
  [광고 제거 · ₩가격]        ← 안 샀을 때. 가격은 스토어가 준 지역 통화 표시. 못 받으면 가격 없이
  [구매 복원]
  [광고 개인정보 설정]        ← 동의 수정이 필요한 지역(UMP 가 요구할 때)만
  광고가 제거되었습니다.       ← 산 뒤에는 이 한 줄만
```

### A.6.1 🔴 비공개 테스트 동안 광고는 꺼 둔다 (2026-09-21 사용자 지시)

사용자 *"출시 전까지 광고는 On 하지말고"* · *"출시 완료되면 그 때 광고 on 하고 광고 자리에는 비공개 테스트동안 영역만 표시해줘"*.

| | 비공개 테스트(지금) | 출시 |
|---|---|---|
| 스위치 | `features/ads/config.ts` 의 **`ADS_LIVE = false`** | `true` |
| 광고 SDK | 🔴 **초기화하지 않는다.** UMP 도 부르지 않는다 | §A.1 흐름 |
| 배너 자리 | **빈 영역만** 그린다(배너 높이 60 · 배경 `tabIndicator`). 레이아웃이 출시본과 같게 보이도록 | 광고 |
| 전면 | 없다 | 복습 끝 |
| 설정의 광고 구역 | **숨긴다**(살 것이 없다 · 상품 등록 전) | §A.6 |
| `AD_ID` 권한 | 🔴 **`blockedPermissions` 로 막는다.** Play 콘솔의 광고 ID 선언 · 광고 포함 · 데이터 보안을 지금 그대로("아니요") 둘 수 있다 | 막지 않는다 · 콘솔 셋을 "예"로 바꾼다 |
| AdMob GDPR 메시지 | **임시저장**(게시 안 함) | 게시 |

- 광고 단위는 **실제 ID 로 바꿔 둔다**(2026-09-21 AdMob 앱 `~9518651929` · 배너 `/8971857014` · 전면 `/9204711174`). 스위치 하나만 켜면 되게 한다.
- 🔴 **스위치는 네이티브가 아니라 JS 다.** 출시 빌드에서 켜려면 `ADS_LIVE` 와 `blockedPermissions` 의 `AD_ID` 를 **한 커밋에** 바꾼다. 권한은 네이티브라 새 AAB 가 든다.
- 가드 `check:ads` ⑧ 이 둘의 짝(꺼져 있으면 `AD_ID` 가 막혀 있어야 하고, 켜져 있으면 막혀 있으면 안 된다)과 꺼져 있을 때 SDK 가 안 깨어나는지 잰다.

### A.7 넣고 난 뒤, 출시 전에 남는 것

AdMob 앱 · 광고 단위 만들기 → ID 교체(A.4) · `app-ads.txt` 확인 · Play 콘솔 상품 `remove_ads` 등록 · 가격(⚠ 미결정 B) ·
Play 데이터 보안(광고 식별자 · 기기 정보 · 구매 기록) · 광고 ID 선언(사용) · 광고 여부(예) · 처리방침 게시 ·
🔴 새 AAB(광고 SDK · `expo-iap` 는 네이티브 모듈) · 새 권한 확인(`check:aab`).

---

## 0. 구현 현황

| 영역 | 상태 | 비고 |
|---|---|---|
| 정책 설계 | 🔄 2026-09-21 | 결정 #29 · #31 · 가격 ⚠ 미결정 B |
| 광고 · 광고 제거 구현 설계 | ✅ 2026-09-21 | §A |
| ~~RevenueCat 프로젝트~~ | 🚫 | 결정 #31 — 일회성이라 필요 없다(My Word 와 같다) |
| ~~스토어 상품 등록(월/연)~~ | 🔄 | 상품 `remove_ads` 하나 · Play 콘솔 등록 ❌ |
| ~~엔타이틀먼트 연동~~ | 🚫 | 결정 #31. 서버 없음. 구매 기록은 Play 에 있다 |
| 구매/복원 UI | ✅ 코드 2026-09-21 | §A.6 · 설정의 광고 줄 · ⏸ 실기기 미확인(새 AAB 필요) |
| 비공개 테스트 동안 광고 끔(§A.6.1) | ✅ 2026-09-21 | `ADS_LIVE = false` · 배너 자리는 빈 영역 · `AD_ID` 막음 |
| 광고(배너 · 전면 · UMP · 연령) | ✅ 코드 2026-09-21 | §A.1~A.4 · 가드 `check:ads` 81검사 · 변이 30종 전부 발화 · 🔴 **테스트 ID** 로 돈다(AdMob 단위 ❌) · ⏸ 실기기 미확인 |
| 처리방침 | ✅ 문안 v1.2 2026-09-21 | §4.2 광고 · §4.3 광고 제거 · §7 Google LLC · 🔴 **게시는 광고 AAB 를 올리기 전에** |
| 법적 고지 세트 | ❌ | Phase 11 |

---

## 1. ~~🔴 왜 광고를 넣지 않는가 (결정 #10)~~ 🔄 뒤집힘 (결정 #29 · 2026-09-21)

> 아래 근거 가운데 2번(기둥 1 · 저장 흐름과 복습 세션)은 **버리지 않고 광고 자리를 고르는 조건**이 됐다 — 그 두 곳과 연령 확인 · 백업 도중에는 광고를 띄우지 않는다(⚠ 미결정 I).

1. 기획서에 광고가 없다 — **없는 것을 발명하지 않는다.**
2. 배너·전면광고는 기둥 1(독서를 방해하지 않는다)과 **정면 충돌**한다. 저장 흐름과 복습 세션은
   이 앱에서 광고를 띄울 수 없는 두 지점인데, 그게 앱 사용 시간의 대부분이다.
3. 형제 앱들은 광고+일회성 구매 조합이지만 그건 **체류형 앱**이다. Re:Read는 짧게 들어왔다
   짧게 나가는 앱이라 노출도 적고 방해는 크다.

⚠ 이 결정을 뒤집으려면 §14에 새 번호로 올리고 기둥 1과의 충돌을 어떻게 해소할지 함께 적는다.

---

## 2. 티어

| | 무료 | Premium |
|---|---|---|
| 책 등록 | 제한 (⚠ A) | 무제한 |
| 지식 저장 | 제한 (⚠ A) | 무제한 |
| 내 생각 | ✅ 무제한 | ✅ |
| **복습** | ✅ **기본 복습**(원문 가리기) | AI 회상 질문 5종 · 순환 |
| **AI 분석** | 🟢 **맛보기 N건**(결정 #14 · ⚠ H) | ✅ 계속 (월 캡 ⚠ E) |
| **로컬 내보내기/가져오기** | 🟢 **✅ 무료**(결정 #15) | ✅ |
| 실천 — **사용자가 직접 쓴 것** | 🟢 **✅ 무료**(결정 #16) | ✅ |
| 실천 — **AI 가 제안한 것** | ❌ (AI 분석에 딸린다) | ✅ |
| 통계 | 기본 대시보드 | + 월간 AI 리포트(v1.2) · 장기 분석 |
| 백업 금고(v1.1 · 자동·암호화·클라우드) | ❌ | ✅ |

🔴 **무료 열에 🟢 셋이 들어온 것이 2026-09-08 기획 검증의 결과다.** 그전에는 무료 사용자가
유료 가치를 **한 번도 보지 못하고**(AI ❌ → 실천 버튼도 안 뜸), **전손 대책도 없었다**(백업 v1.1 유료).
가치·이탈·정합 세 렌즈가 독립적으로 같은 곳을 짚었다. 근거는 `../CLAUDE.md` §14 #14·#15·#16.

### 2.1 🔴 무료에서도 저장과 복습이 성립해야 한다 (결정 #9)

- 복습이 Premium 전용이 되면 **기둥 7이 깨진다** — 그럼 이 앱은 그냥 메모장이다.
- "책을 저장하려고 돈을 내는 게 아니다"(기획서 §28)가 정책의 기준선이다.
- → 무료 한도(⚠ A)는 **체험판이 되지 않을 만큼** 넉넉해야 한다. 너무 낮으면 위 문장이 거짓이 된다.
  ⚠ 미결정 A를 정할 때 이 문장을 기준으로 판단한다.

### 2.2 🔴 한도에 걸렸을 때의 동작

- **이미 저장한 것은 절대 잠그지 않는다.** 한도는 **새로 만드는 것**에만 적용된다.
- 무료로 내려온(해지한) 사용자의 기존 데이터는 전부 그대로 보이고 복습된다.
  🚫 "무료 한도 초과 — 12개가 잠겼습니다" 같은 인질 모델을 쓰지 않는다.
- 한도 안내는 저장 **직전**에 뜬다. 다 쓰고 나서 저장이 실패하는 흐름은 만들지 않는다.

### 2.3 해지 후

| | 해지 후 |
|---|---|
| 기존 AI 분석·회상 질문 | ✅ **로컬에 있으므로 계속 쓴다** |
| 기존 실천·체크 | ✅ 계속 동작 |
| 새 AI 분석 | ❌ |
| 새 실천 생성 | ❌ |
| 새 책·지식 | 무료 한도 적용 |
| 백업(v1.1) | 복원은 ✅ · 새 백업 ❌ + 유예 90일(조각 승계) |

🔴 이게 로컬 정본의 장점이다 — **산 것은 남는다.** 서버 정본이면 해지 시 접근을 끊게 되는데,
그건 사용자가 몇 년치 지식을 인질로 잡히는 구조다.

---

## 3. 상품

| 상품 | 유형 | 비고 |
|---|---|---|
| Premium 월간 | 자동갱신 구독 | 가격 ⚠ B |
| Premium 연간 | 자동갱신 구독 | 가격 ⚠ B · 월간 대비 할인 |

- 무료 체험은 **결정 #14**(AI 맛보기)로 닫혔다 — 시간제 체험이 아니라 **분석 N건**이다.
- 🔴 **글로벌 가격이다.** 국가별 티어를 스토어에 맡길지 직접 지정할지 Phase 10에서 정한다.
  `common/BUSINESS_INFO.md`에 형제 앱의 가격 정책이 있으면 먼저 참조한다.

---

## 4. 결제 · 엔타이틀먼트 경로

```
앱 → RevenueCat SDK → 스토어 결제
                          │
                          ▼
        RevenueCat → common_server /api/webhooks/revenuecat/reread
                          │
                          ▼
                    entitlements 갱신
                          │
앱 → common_server /api/v1/entitlements ◀─┘   (진실)
Re:Read 서버 → common_server /api/v1/entitlements  (AI 게이트)
```

- 🔴 **진실은 common_server 엔타이틀먼트다.** 앱의 로컬 캐시는 UI용이고, **AI 게이트는 서버가 판정한다**
  (`AI_SYSTEM.md` §5). 클라이언트 게이트만 두면 우회된다.
- 🔴 **만료는 이벤트로 오지 않는다.** common_server는 `active`를 저장하지 않고 읽을 때 계산한다 —
  만료 순간에 DB를 쓰는 주체가 없다. 유예 기간이 필요한 기능(백업 v1.1)은
  **만료 시각 스냅샷을 우리 서버에 저장하고 우리가 센다**(조각 승계).
- **복원(Restore)** 경로를 반드시 둔다. 기기 교체·재설치 시 필요하고 스토어 심사 항목이다.

---

## 5. 법적 의무 세트

🔴 **글로벌 출시이므로 한국 기준만으로 부족하다.** 정본은 `C:\project\common\GLOBAL_DATA_COMPLIANCE.md`.

| 항목 | 필요한 것 |
|---|---|
| 자동갱신 고지 | 갱신 주기·금액·해지 방법을 **구매 화면에** 명시 |
| 해지 경로 | 앱 안에서 스토어 구독 관리로 가는 링크 |
| 환불 | 스토어 정책 안내 + 문의 경로 |
| 미성년자 결제 | 연령 게이트(⚠ 미결정 G)와 연결 |
| 가격 표시 | 국가별 현지 통화 · 세금 포함 여부 |
| EEA | GDPR 고지 · 구독 철회권(Digital Content Directive) |
| 미국 | 주법별 고지(CCPA/CPRA 등) |
| 한국 | 전자상거래법 표준약관 · 청약철회 |
| **탈퇴** | 🔴 **앱 내 경로 + 웹 URL 둘 다.** 하나만 있으면 반려된다(`GLOBAL_DATA_COMPLIANCE.md` §6.1) |
| 🔴 **계정 삭제 ≠ 구독 해지** | 탈퇴 화면에 **별도 해지 안내**. 계정만 지우고 구독이 살아 있으면 **과금이 계속된다** — 그게 분쟁이 된다 |
| Play 데이터 보안 | ⚠ **RevenueCat SDK 가 보내는 것도 "공유"로 선언**한다. *"우리 코드가 안 보낸다"* 는 근거가 안 된다(같은 문서 §6) |
| 웹에 있어야 하는 것 | 처리방침 · 이용약관 · **계정 삭제 안내 URL** — 셋 다 Play 콘솔 등록 + **200 응답 실측**(같은 문서 §8) |

⚠ 법률 문서(처리방침·약관)는 Phase 11에서 `LEGAL_SYSTEM.md`로 분리한다.
🔴 **AI 고지가 처리방침에 들어간다** — "원문이 AI 제공자에게 전달되며 저장되지 않는다"
(`AI_SYSTEM.md` §2.1). 이건 게시 **전에** 확정돼야 한다.

---

## 6. 구현 시 함정 (형제 실증 승계)

- 🔴 **릴리스 AAB를 로컬에서 구울 때 개발용 env가 번들에 문자열로 박힌다**(조각 실증).
  EAS는 깨끗한 컨테이너라 없는 문제인데 로컬은 `.env.local`을 읽는다. **올린 뒤에는 못 고친다.**
  → 릴리스 빌드 전 env 검사 스크립트를 만들고 [`README.md`](./README.md) §3 검증 루틴에 넣는다.
- RevenueCat SDK는 **네이티브 모듈**이라 Expo Go에서 안 된다. 이 단계부터 dev build.
- 🔴 **스토어 상품 등록과 결제 빌드가 서로를 전제한다** — `common/PRE_LAUNCH_CHECK.md` §3 실측:
  *"결제 코드·빌드·업로드가 먼저, 콘솔 상품 등록이 그다음"*. 즉 Phase 10(구독)은 Phase 11의
  Play 콘솔·서명 키를 일부 **앞당겨** 써야 한다. 순서를 모르고 들어가면 Phase 10에서 막힌다.
- 🔴 **약관 게시가 결제 빌드보다 먼저다**(같은 문서 §2): ① 약관 게시 → ② 처리방침에 인앱 결제 항목
  신설·재게시 → ③ **그다음** 결제 빌드를 심사에 올린다. 우리 Phase 순서(10 결제 → 11 법무)와 반대다.
- 샌드박스 결제·환불 테스트 절차는 `store-iap-setup` 스킬 참조.
