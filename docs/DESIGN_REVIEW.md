# DESIGN_REVIEW — 화면 시안 · UI/UX · 문구 검수 (ChatGPT 협업)

> 🔴 **이 프로젝트의 디자인·문구 검수는 ChatGPT 와 함께 한다.** 사용자 지시(2026-09-09):
> *"chatgpt 크롬으로 진행하라고 하면 이 채팅방을 이용해줘"*
>
> 이 문서는 **창구와 규칙**, 그리고 **거기서 나온 결론**을 담는다.
> 시안 그림 자체가 아니라 **채택한 값과 이유**가 여기 남아야 한다 — 그림은 채팅에 있고 우리는 값을 쓴다.

---

## 0. 창구 — 고정된 채팅방 하나

| | |
|---|---|
| 서비스 | ChatGPT (크롬) |
| 방 | 사이드바 **고정됨 → `Bookmind`** |
| URL | `https://chatgpt.com/c/<ChatGPT 방 ID>` |

- 🚫 **새 채팅을 만들지 않는다.** 그 방에 기획서 원문과 이름 결정 논의가 이미 쌓여 있어서,
  새 방을 파면 상대가 맥락을 모른 채 답한다.
- 🔴 방을 못 찾으면 **사용자에게 묻는다.** 비슷한 이름의 다른 방에 올리지 않는다.

## 1. 무엇을 물어보나

| 축 | 왜 ChatGPT 인가 |
|---|---|
| **화면 시안** | 이미지로 뽑아 보는 것이 말로 설명하는 것보다 빠르다 |
| **UI/UX 피드백** | 우리가 만든 화면을 우리가 보면 익숙해져서 안 보인다 |
| 🔴 **모든 화면 문구** | 사용자 지시(2026-09-09): *"모든 화면 텍스트 문장들은 솔직히 claude가 너무 별로야"* — 이 프로젝트만이 아니라 **다른 프로젝트에서도 같은 문제**라는 지적이다. 문구는 반드시 검수를 받는다 |

### 🔴 1.1 답은 **캔버스 문서로** 받는다 (2026-09-09 사용자 지시)

대화창 답변은 **중간에 끊긴다.** 실제로 UI/UX 피드백이 `"상단의 문장"` 에서 잘려 두 번 이어 받아야 했고,
그렇게 받은 조각은 옮겨 적기도, 나중에 다시 읽기도 어렵다.

```
요청할 때 같이 적는다:
  · 캔버스 문서 하나로, 끊기지 않게 한 번에
  · 그대로 복사해 갈 수 있는 마크다운으로
  · 항목마다 [무엇이 문제인가] / [왜 문제인가] / [어떻게 고치나] / [RN 스타일 값]
```

⚠ 그리고 **문서로 받은 것을 그대로 우리 저장소에 붙여 넣지 않는다.** §3 에 **결정만** 옮긴다 —
받은 문서는 의견이고, 우리 문서는 결정이다.

⚠ **받는 것은 의견이지 결정이 아니다.** 기둥(`CLAUDE.md` §2)과 부딪히는 제안은 기각하고
그 사유를 아래 §3 에 남긴다 — 남기지 않으면 다음 세션이 같은 제안을 다시 받아온다.

## 2. 🔴 제약을 먼저 말한다 — 안 그러면 못 쓰는 시안이 온다

시안을 부탁할 때 **반드시 함께 보내는 것**:

```
· React Native(Expo)다. 웹 CSS 가 아니다
    box-shadow → iOS shadow* + Android elevation
    linear-gradient → expo-linear-gradient 를 깔아야 한다(기본 없음)
    backdrop-filter · filter · ::before/::after → 없다
· 🔴 번들 폰트를 쓰지 않는다(결정 #13). 폰트 파일이 필요한 시안은 채택 불가다
    사용자가 저장하는 책 문장의 언어를 우리가 못 정하므로 번들 폰트는 두부(□)를 만든다
· 라이트/다크 두 벌
· 현재 토큰 값(색·간격·라운드·타이포)을 그대로 붙여 준다
```

그리고 **요소마다 "에셋이냐 스타일 값이냐"를 갈라 달라고 요구한다.**
🟢 에셋 없이 되는 쪽을 우선한다 — 앱 용량과 다국어 때문에 파일을 늘리지 않는다.

## 3. 결론 (채택·기각)

> 여기가 이 문서의 본체다. 대화가 아니라 **결정**을 적는다.

| 날짜 | 무엇 | 채택/기각 | 근거 |
|---|---|---|---|
| 2026-09-09 | 화면 시안 5장(홈·복습 가림/공개·문장 상세·책 상세) | 🔨 **검토 중** | 톤은 참고한다. 다만 시안이 **책 표지·독서 진행률·간격 노출**을 전제했는데 앞의 둘은 우리 MVP 범위 밖이었다 — 진행률만 결정 #17 로 편입했다 |
| 2026-09-09 | 이미지 에셋 필요 여부 | ✅ **채택** | *"필수 이미지 에셋 없음 — 아이콘은 라이브러리(lucide-react-native)로 충분"*. 🟢 앱 용량·다국어 부담을 안 늘리고 간다 |
| 2026-09-09 | 🔴 **복습 화면이 플래시카드로 보인다** — 지시문·입력이 각각 카드 | ✅ **채택** | 카드 두 개가 나란히 놓이면 사용자가 *"이 카드는 뭐지"* 를 먼저 해석한다. 지시문을 일반 텍스트로 내리고 입력은 조용한 공간으로 |
| 2026-09-09 | 🔴 **홈에서 저장 버튼이 복습과 같은 무게** | ✅ **채택** | 둘 다 큰 파란 버튼이라 "저장하는 앱"으로 읽힌다. **위치는 그대로, 색만 낮춘다** — 위치는 기능 우선순위, 색은 제품 우선순위 |
| 2026-09-09 | 🔴 **빈 상태에서 0을 강조하지 않는다** | ✅ **채택** | 신규 사용자가 `책 0 · 문장 0` 을 보면 성취 대시보드가 된다. 숫자 대신 중립적인 문장으로. 🚫 `첫 문장을 저장해보세요!` 같은 유도 문구는 안 넣는다(기둥 5) |
| 2026-09-09 | 🟠 **내 생각이 원문에 딸린 메모처럼 보인다** | ✅ **채택** | 🔴 크기로 해결하지 않는다(원문이 주인공인 것은 맞다) — **독립 영역**으로 분리하고 작성 전/후를 나눈다 |
| 2026-09-09 | 🟠 **카드 남용** — 거의 모든 요소가 흰 카드 | ✅ **채택** | 카드가 정보를 구분하는 게 아니라 위계를 평평하게 만든다. 4단계로: 원문=카드 / 내 생각=약한 면 / 메타데이터=텍스트 / 행동=버튼 |
| 2026-09-09 | 🟡 **책 상세의 0** | ✅ **채택** | 빈 책의 `문장 0 · 생각 0 · 복습 0 · 실천 0` 은 진행이 아니라 **미완성**으로 보인다 → 데이터가 생긴 뒤에만 통계를 낸다 |
| 2026-09-09 | 🟡 **색상에 의미를 더하지 않는다** | ✅ **채택** | Accent=주요 행동, Danger=삭제만. 🚫 복습 4등급에 신호등 색을 주지 않는다 — 결국 점수로 보인다 |
| 2026-09-09 | 책 표지 자리표시자(제목 첫 글자 + 고정 색) | ✅ **채택** | 에셋 0KB. 🔴 **색은 랜덤이 아니라 등록할 때 배정해 저장**한다 — *"Atomic Habits = 파란 A"* 라는 기억이 생기기 때문 |
| 2026-09-09 | 🔄 **원문 앞 12자가 너무 강한 단서** | ✅ **채택(우리 결정을 되돌림)** | `9월 10일 / Systems beat goals...` 이면 거의 알아본다 — **회상이 아니라 인식**이 된다. 8자로 줄인다. ⚠ 오늘 §3.1 을 정할 때 "너무 짧으면 막막하다"만 봤고 **반대쪽 위험을 안 봤다** |
| 2026-09-09 | 4등급 버튼 아래 부가 설명 — 시안의 `더 자주 / 조금 더 / 조금 더 뒤에 / 한참 뒤` | 🚫 **내용 기각 · 형식 채택** | 선택 **전**에 스케줄을 보여주면 등급이 회상 보고가 아니라 스케줄 다이얼이 되어 FSRS 입력이 오염된다(기둥 5 · §5 규칙 4). 실측이 `1분/6분/10분/8일`이라 "더 자주"도 부정확하다 → 같은 자리에 **기억이 언제 돌아왔는지**를 적는다(`REVIEW_SYSTEM.md` §1.1) |
| 2026-09-09 | 🔴 **전 화면 문구 검수**(88키 전문 전달 → 바꿀 것 28키 회신) | ✅ **대부분 채택** | 사용자 지시 §1. 받은 문서는 `scratchpad/ReRead_문구_검수.md`, 실제 반영은 22키(ko)·20키(en). 축은 셋이었다 — ① 시험·과제 어휘 제거(`due`·`Forgot`·`Easy`·`몫`) ② 명령형 제거(`적어 보세요` → `떠오른 내용`) ③ 같은 개념을 같은 말로(`복습할 것`·`떠올릴 것` → **다시 만날 문장**) |
| 2026-09-09 | 4등급 라벨을 회상 경험으로 바꾼다 | ✅ **채택** | `잊었다/애매하다/기억났다/쉽다` → `안 떠올랐다/겨우 떠올랐다/떠올랐다/바로 떠올랐다`. `쉽다`는 **문제의 난이도**를 평가하는 말이라 시험 앱이 된다 — 우리가 재는 것은 문제가 아니라 **기억이 언제 돌아왔는가**다 |
| 2026-09-09 | 🚫 `hard` = **보고서야 떠올랐다** | 🚫 **기각** | 우리 화면은 **원문을 공개한 뒤에** 등급을 고른다 — *"보고서야"* 는 네 버튼 전부에 해당해 가르는 말이 못 된다. 뜻으로도 `again` 에 가까워 `hard` 자리에 놓으면 FSRS 입력이 흐려진다 → `겨우 떠올랐다` 로 다시 썼다(`REVIEW_SYSTEM.md` §1.1) |
| 2026-09-09 | `review.ratingHint.*` 부제를 만들지 않는다 | 🔄 **우리 결정을 되돌림** | 라벨이 회상 경험을 직접 말하게 되면서 부제는 **같은 말의 반복**이 됐다. 아침에 정한 §1.1 의 부제 표가 스스로 세운 규칙("라벨과 같은 말을 되풀이하지 않는다")을 어기고 있었다 |
| 2026-09-09 | `knowledge.save.placeholder` = *"남기고 싶은 문장"* | ✅ **채택** | ~~"무엇을 발견했나요?"~~ 는 입력 범위가 너무 넓어 무엇이든 적으라는 칸으로 읽힌다. ⚠ 기둥 1(원문 하나면 끝)을 **좁히지 않는다** — 안내일 뿐 검증이 아니다 |
| 2026-09-09 | `books.new` 등록 → **추가** | ✅ **채택** | `등록`은 서비스 관리 화면의 말이다. 우리 화면은 사용자가 자기 책장에 한 권 얹는 것이다 |
| 2026-09-09 | `books.status.done` 완료 → **다 읽음** | ✅ **채택** | 책의 상태이지 작업의 완료가 아니다 |
| 2026-09-09 | `ai.notice.proxy` 재작성 | ✅ **채택** | ~~`해당 지식의`~~ 는 제품 내부 용어이고, en 의 `your note` 가 제품 용어 `내 생각`(thought)과 어긋나 있었다. 🔴 **사실관계는 한 글자도 안 바뀌었다** — 고지문은 문장이 바뀌어도 내용이 바뀌면 안 된다 |
| 2026-09-09 | 🔴 **홈 개편 — 카드를 줄이는 게 아니라 "카드처럼 보이는 것"을 줄인다** | ✅ **채택** | 대표님 지적(*"어디에 시선을 집중해야 하는지 모르겠다"*)에서 출발. 블록 9개가 전부 같은 무게였고 둥근 테두리 상자가 **6개**였다. 홈을 네 영역(Header / Today / Recent / Meta)으로 줄인다 |
| 2026-09-09 | 태그라인·저장 힌트를 홈에서 뺀다 | ✅ **채택** | 둘 다 **처음 한 번 읽으면 끝**인데 매일 가장 좋은 자리를 먹고 있었다. 저장 힌트는 저장 화면에 그대로 남아 있어 잃는 것이 없다 |
| 2026-09-09 | 🔴 **복습이 없는 날에는 저장 버튼을 primary 로 바꾼다** | ✅ **채택(우리가 못 본 것)** | 우리 진단은 *"0건이면 화면에 색이 하나도 없다"* 에서 멈췄는데, 답은 **색을 없애지 말고 옮기는 것**이었다. 🟢 화면에 accent 가 **언제나 정확히 하나** 있게 된다. ⚠ 위치·크기는 그대로라 "다시 열었을 때의 핵심 행동은 복습"(기둥 7)도 안 깨진다 |
| 2026-09-09 | 최근 저장에서 카드를 걷어내고 구분선만 남긴다 | ✅ **채택** | 항목 전체는 `Pressable` 로 두어 터치 영역은 유지한다. 🔴 **시각적으로 카드가 아니어도 기능적으로는 목록**이다 |
| 2026-09-09 | 최근 항목에 출처 한 줄(`책 · 저자`)을 붙인다 | ✅ **채택** | 카드 테두리를 걷어내면 문장들이 서로 붙어 보인다. 출처가 **구분자 겸 맥락** 역할을 한다. 없으면 억지로 만들지 않는다 |
| 2026-09-09 | 🔴 **책·문장 통계** — (a)텍스트 한 줄 (b)홈에서 제거 (c)유지 | ✅ **(a) 채택** | 대표님이 *"시안 보고 결정"* 으로 미뤄 둔 안건. 근거: ① 축적을 보여주는 정보라 없앨 정도는 아니다 ② 지금 형태는 **버튼처럼 보이는데 성격은 아니다** ③ 크게 두면 제품이 "수집량 중심"으로 읽혀 기둥 5 와 어긋난다 |
| 2026-09-09 | ⚠ 통계 줄을 **누를 수 있게** 둔다(시안은 정보로만 제안) | 🔧 **수정 채택** | 시안대로 순수 텍스트로 두면 **책 목록·문장 목록으로 가는 유일한 길이 사라진다**(홈에 다른 진입점이 없다). 상자만 걷어내고 텍스트 링크로 남긴다 |
| 2026-09-09 | 섹션 제목을 `label 13/muted` → `18/26 600/text` | ✅ **채택** | `오늘의 복습`·`최근 저장` 이 본문보다 작고 흐려서 **구획이 안 보였다**. 위계는 크기·색으로 만든다 |
| 2026-09-09 | 좌우 여백 16 → **32** | ⏸ **보류** | 시안은 홈 기준 32 를 권했다. 🔴 홈만 바꾸면 화면 간 여백이 어긋난다 — `Screen` 전체를 바꾸는 결정이라 **다음 화면 패스에서 한 번에** 본다 |
| 2026-09-09 | 헤더 로고 `title 22` → `logo 32/40 700` | ⏸ **보류** | 같은 이유. `Header` 는 전 화면 공용이라 32 로 올리면 상세 화면 제목까지 커진다. 홈 전용 변형이 필요해지는데 그건 이번 범위가 아니다 |
| 2026-09-14 | 🔴 헤더 로고 — **홈만** `brand 32/40 700` · 나머지 화면은 `title 22/30 700` 유지 | ✅ **채택** | 9/9 보류 사유(*`Header` 는 전 화면 공용이라 상세 제목까지 커진다*)를 `Header variant="brand"` 로 풀었다. 로고 에셋은 **0KB**(시스템 폰트 글자). 토큰 `typography.brand` 는 홈의 앱 이름 한 곳만 쓴다 |
| 2026-09-14 | 🔴 **하단 탭 다섯**(홈 · 문장 · 책 · 실천 · 설정) | ✅ **채택(사용자 지시 · 결정 #26)** | *"홈 화면에 다 몰려있어서 쫌 보기 힘들다"*. 9/9 홈 개편의 Meta 줄(책·문장·통계·실천 링크)과 헤더의 검색·설정 링크가 탭으로 대체된다. 🔴 탭 아이콘 없음(0KB) · 활성 탭은 accent 가 아니라 **진한 글자**(탭 바가 모든 화면에 붙어 accent 가 둘이 되는 것을 막는다) |
| 2026-09-14 | 🔄 하단 탭 디자인 — **아이콘 + 글자 · 선택은 엷은 알약 배경** | ✅ **채택(사용자 선택)** | 사용자 *"하단 네비게이션 디자인 너무 별로인데? 다른 앱들 참고해볼래"*. 참고: Material 3 내비게이션 바(높이 80dp · 아이콘 24dp · 선택 표시 56×32dp 알약 · 3~5개 · Material Components Android 문서) · iOS 탭 바(49pt · 아이콘 25pt) · 탭 바 설계 글(아이콘+글자 권장 · 대비 3:1). **글자만 있는 탭은 어느 쪽도 권하지 않았다.** 🔴 알약은 accent 가 아니라 **중립 종이색**(`palette.tabIndicator`) · 9/9 의 "아이콘을 안 넣는다"를 **탭 바에서만** 바꾼다 · 대가: `react-native-svg` 네이티브 추가 |
| 2026-09-14 | 🔄 **화면 위 오른쪽 아이콘**: 추가(+) · 저장(✓) · 수정(연필) · 삭제(휴지통) · 취소(×) · 검색 · 통계 | ✅ **채택(사용자 지시)** | 사용자 *"등록, 수정 같은 경우 우측 상단에 아이콘으로 전체적으로 놔둘래 너무 산만하다 삭제도 포함이야(휴지통 아이콘)"*. 범위는 사용자 선택으로 **모든 입력·상세 화면**(책 · 문장 · 실천의 탭 · 만들기 · 상세). 부품 `IconButton` 하나로 그린다(Lucide 22 · 누르는 자리 40). 🔴 **삭제는 확인창을 그대로 거친다.** 아이콘이 작아진 만큼 잘못 누르기 쉬운 자리다. 휴지통은 `danger` 색이 아니라 글자색이다(확인창의 빨간 버튼이 그 역할을 한다). ⚠ 9/9 의 "아이콘은 탭 바에만"을 **헤더 오른쪽까지** 넓힌다. 본문 안의 행동(태그 추가 · 생각 추가 · 그만두기 · 사진에서 가져오기 · 실천 제안)은 글자 버튼으로 남는다 |
| 2026-09-14 | 🚫 **책 표지 자리(첫 글자 + 색 상자) 제거** | ✅ **채택(사용자 선택)** | 사용자 *"책 표지 저장하는 곳도 없는데? 그걸 넣어주던지 아니면 제거하던지 선택하자"*. 9/9 에 채택한 자리표시자가 **사진을 넣을 수 있는 자리처럼 보였다.** 표지 등록은 ISBN · 표지 자동 등록과 함께 2차(`CLAUDE.md` §3)라 넣지 않고 뺀다. `BookCover` 부품과 `coverInk` 토큰을 지웠다. 🟢 DB 의 `cover_color` · `cover_uri` 컬럼은 **그대로 둔다**(Expand-only · 2차가 쓴다) |
| 2026-09-14 | 문장 목록 **등록순 / 책별** 고르기 · 기기에 저장 | ✅ **채택(사용자 지시)** | 규칙은 `KNOWLEDGE_SYSTEM.md` §3.2. 고르기는 목록 위 칩 두 개다. 문장 탭에서 저장 버튼이 아이콘으로 올라가 선택된 칩이 이 화면의 accent 하나가 된다 |
| 2026-09-14 | 홈 버튼 높이 12 → **16** · 구분선 머리카락 → **1dp · 새 토큰 `divider`** | ✅ **채택(사용자 지시)** | *"오늘의 복습, 문장 저장하기 버튼 높이 조금 높혀줘"* · *"최근 저장 영역에 각 문장들끼리 뭔가 실선으로 되어있는데 너무 희미해서 잘 안보여"*. 버튼은 `Button size="lg"`(위아래 여백 `spacing.lg`)이고 홈에만 쓴다. 선은 카드 테두리색 `border` 를 머리카락 두께로 쓰던 것을 **1dp · 한 단계 진한 `divider`**(light `#CFC8BB` · dark `#4A463B`)로 나눴다. 🔴 카드 테두리는 안 바꾼다(카드가 무거워진다) |
| 2026-09-14 | 실천 **기록 달력**: 한 달 · 지난 날도 체크 | ✅ **채택(사용자 선택)** | 규칙은 `PRACTICE_SYSTEM.md` §3.1. 채운 동그라미 = 한 날 · 오늘은 테두리 · 누를 수 없는 날은 흐리게(주 칸과 같은 표시). 실천 상세의 이번 주 칸을 달력으로 바꿨고, 실천 탭의 오늘 칸은 그대로다 |
| 2026-09-14 | 🔄 홈 헤더 `Re:Read` **제거** | ✅ **채택(사용자 지시)** | 같은 날 채택한 `brand 32/40`(위 행)을 뒤집는다. 쓰는 곳이 없어져 토큰 `typography.brand` 와 `Header variant` 도 뺐다 |
| 2026-09-14 | 화면 위 여백 = **인셋 + 16** | ✅ **채택(사용자 지시)** | *"윗 쪽 여백 쫌 줘야할 것 같아 전체적으로"* · 연령 확인 모달에서 먼저 짚었다. 좌우 16 과 같은 값 · `Screen` 한 곳이라 전 화면이 같이 바뀐다 |
| 2026-09-14 | 실천 칸 — 누를 수 없는 날은 **흐리게**, 오늘 요일은 **굵게** | ✅ **채택** | 사용자가 *"모든 요일 클릭할 수 있게 보이는데 실제로 해당 요일만 클릭 가능한 거 같은데"* 라고 짚었다. 판정은 있었고 모양만 안 따라왔다. 🚫 색은 안 쓴다(accent 하나 · 기둥 5) |
| 2026-09-14 | 🔴 좌우 여백 — **16 유지**(9/9 보류를 닫는다) | ✅ **결정** | 첫 답은 **현재값을 32 로 잘못 읽고** 32 를 권했다. 16dp 라는 사실(기기 약 411dp · 본문 379dp)을 주고 다시 묻자 16 유지로 바뀌었다. 근거: 사진 화면의 버튼 두 개가 32 에서는 각 167.5dp 로 좁아지고, `quote 18/28` 은 379dp 에서도 한 줄이 과하게 길지 않다. 🔴 **첫 권고를 그대로 옮겼으면 틀린 전제로 전 화면을 바꿨다.** ★ 받은 의견의 **전제**부터 대조한다 |
| 2026-09-14 | OCR 문자 링크 — en `Read as a different script` 유지 · ko `다른 문자로 다시 읽기` → **`다른 문자로 다시 인식`** | ✅ **채택** | "읽기" 는 사용자가 읽는 행동으로도 읽혀서 사진을 다시 인식한다는 뜻이 흐리다. 첫 답이 권한 en `Try another character set` 은 **기각**: character set 은 문자 인코딩(charset)으로도 읽힌다. 재확인에서 en 은 현재 문구 유지로 정리됐다 |
| 2026-09-14 | OCR 문자 링크 색을 accent 로 | ❌ **기각** | 같은 화면의 [Take a photo] 가 이미 accent 라 **둘이 된다**(한 화면 accent 하나). `textMuted · caption 12/16` 으로 둔다. 재확인에서 ChatGPT 도 동의했다 |
| 2026-09-09 | 다크 모드에 카드·glow 를 더하지 않는다 | ✅ **채택** | 레이아웃을 라이트와 **완전히 동일**하게 둔다. accent 를 빛나는 효과로 쓰지 않는다 |
| 2026-09-09 | 에셋 | ✅ **또 0KB** | 로고·일러스트·배경 전부 안 쓴다. 아이콘도 선택이고 *"요소가 이미 많으니 더하기보다 빼는 쪽"* 이라 이번엔 안 넣는다 |
| 2026-09-09 | 🔴 **앱·알림 아이콘 A안 — 두 페이지 + 콜론** | ✅ **채택** | 두 장의 페이지가 마주 보고 **가운데 여백이 `Re:Read` 의 콜론**을 만든다. 책 아이콘을 직접 그리지 않고 책 맥락을 전한다. 기각된 둘: B안(펼쳐지는 페이지)은 *"책"* 을 너무 직접 말하고, C안(순환 고리)은 refresh·sync 아이콘으로 읽힌다 |
| 2026-09-09 | 아이콘 색 — 배경 `#3A5A73` · 전경 `#FBFAF7` | ✅ **채택** | 앱 화면은 종이톤이지만 **런처 아이콘은 뒤집는다.** 홈 화면에서 앱을 찾는 것은 색 대비이지 톤이 아니다 |
| 2026-09-09 | 🔴 **알림 아이콘을 앱 아이콘의 축소판으로 쓰지 않는다** | ✅ **채택(우리가 몰랐던 구분)** | 안드로이드 small icon 은 **알파 채널만 쓰고 색을 전부 버린다.** 축소판을 넣으면 뭉갠다. 별도 흰 실루엣(96px)이 필요하다 |
| 2026-09-09 | 적응형 배경을 이미지로 만들지 않는다 | ✅ **채택** | `backgroundColor` 값 하나로 끝난다. 🟢 만들 파일이 하나 줄었다 |
| 2026-09-09 | Android 13+ `monochromeImage` 도 낸다 | ✅ **채택** | 테마 아이콘을 안 내면 OS 가 알아서 만들지 않고 **그냥 옛 아이콘이 뜬다** |
| 2026-09-09 | 🔧 **SVG 원본은 우리가 그렸다** | 🔧 **수정 채택** | 🔴 ChatGPT 가 SVG 코드블록을 **두 번 다 빈 채로** 보냈다(라벨만 `SVG`, 내용 0자). 오늘 이미지 생성이 조용히 실패한 것과 같은 증상이다. **컨셉·색·규격·`app.json` 은 전부 받았으므로** 그 사양대로 도형을 직접 그렸다(`scripts/make-icons.mjs`). ⚠ 그림의 완성도는 **대표님 판단 대기**다 |

## 4. 조작 함정 — `common/ASSET_GENERATION.md` §5 를 그대로 따른다

형제 프로젝트들이 이미 겪은 것들이라 **다시 밟지 않는다**:

- 🔴 **프롬프트를 타이핑하지 않는다.** 한글 IME 가 공백을 흘리고 음절을 깨뜨린다
  (`횃불` → `훃불`, 재현율 100%). `document.execCommand('insertText', …)` 로 통째 삽입한다.
  🟢 덤으로 **넣은 값을 되읽어 대조**할 수 있다 — 스크린샷으로 눈 확인하는 단계가 사라진다.
- 전송도 좌표 클릭이 아니라 `[data-testid="send-button"]` 클릭이다(입력이 길면 버튼이 밀린다).
- `ctrl+a` 는 포커스가 입력창 밖이면 **페이지 전체**를 잡는다.
- 이미지 저장의 ⬇ 는 **공유 모달**이다. 외부 공유 버튼을 누르지 않는다.
- 파일 첨부는 클릭하지 않는다(네이티브 대화상자가 뜨면 자동화가 멈춘다) — 파일 입력에 직접 넣는다.

---

*최종 갱신: 2026-09-09 — 문서 신설. 창구·제약·함정을 먼저 박아 두고 첫 시안을 요청했다.*
